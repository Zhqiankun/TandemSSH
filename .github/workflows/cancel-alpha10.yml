name: Cancel superseded alpha.10 preview
on:
  push:
    branches: [codex/unix-permissions]
    paths: [.github/workflows/cancel-alpha10.yml]
permissions:
  actions: write
jobs:
  cancel:
    if: github.repository == 'Zhqiankun/TandemSSH'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Cancel only the superseded preview run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          status=$(gh api repos/Zhqiankun/TandemSSH/actions/runs/34678529650 --jq .status)
          if [ "$status" != "completed" ]; then
            gh api --method POST repos/Zhqiankun/TandemSSH/actions/runs/34678529650/cancel
          else
            echo "The targeted run is already completed; inspect its conclusion."
          fi
