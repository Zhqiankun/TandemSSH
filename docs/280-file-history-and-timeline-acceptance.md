# 文件失败历史与执行时间线验收归并

2026-09-13，R05 / F08。主智能体独立验证；本轮新增网关→磁盘历史测试，无生产代码修改。

新增 18 组：automatic / collaborative × mcp / agent / workflow × failed / takeover / timeout。使用真实 OperationGateway、SessionControl、AuditJournal，文件执行端口注入权限失败、首个分块后的接管或 1 秒超时。网关实际产生日志后重新创建 AuditJournal，从磁盘查询与读取详情，核对来源、模式、主机、路径、动作、策略版本、状态、失败原因；文件结果没有伪造退出码或终端输出。接管/超时不执行第二块，终端写入为空。

这些是实际网关与磁盘集成测试，文件 I/O 端口受控；不能称为本轮真实 SFTP 权限或断网测试。真实 Linux 文件故障与恢复证据分别见 254、265，不能由本测试取代。

联合回归：task-runtime、file-gateway、audit-journal、history-provenance、TaskHistory 共 5 文件 126 项全部通过，无跳过。TypeScript 与本轮测试 ESLint 通过。日志 .cache/history-combined-validation.log、file-history-tsc.log、file-history-lint.log。

按原始范围核对：

| 原始检查点 | 当前直接证据 |
| --- | --- |
| 人 / AI / MCP 来源 | task-runtime 的六条来源/模式链真实写入再读回（143）；人工授权/审批来源独立；MCP 历史中文桌面（79），AI 与人工审批重启 HTTP 读回（279） |
| 目标、命令与文件操作 | 可信主机快照、会话、cwd、命令/路径与动作投影测试（79/143）；文件 read/write 磁盘往返及本轮失败矩阵 |
| 执行状态、输出与失败原因 | 成功、失败、unknown 命令的运行时→日志往返；本轮文件权限失败/接管/超时；history-provenance 中文失败原因、未知状态与文件不确定提交展示 |
| 审批及策略版本 | human 授权/单次审批与操作策略版本、判断真实往返（143）；unknown 判断保留（142）；协作任务重启读取 operation.approval（279） |
| 中文只读查看及持久化 | 79 的实际 Windows MCP 历史，277 的取消/确认归档/退出后落盘，278 的中文摘要，279 的同一 profile 重启后筛选、摘要与原始详情；服务器关闭仍可查阅 |
| 保留原始未知结果 | 276 的精确人工确认集合及无重执行，277/279 的真实 cancelled / unknown ID 保留；文件未知和审计缺口仍禁止由命令确认绕过 |

结论：R05 原文“命令、文件操作、执行者、状态、输出与失败原因可追踪”和 F08 原文“人/AI/MCP 来源、目标、操作、状态、输出片段、审批及策略版本”已有对应实现和验证，均标记 verified，总计 37/79。

此结论是执行记录与展示验收，不将它扩展为所有文件传输、恢复、清理功能完成；这些仍归 F09 / B 系列原条目验收。人工普通终端保持自由接管，完整终端录制仍遵循用户已选择的默认关闭策略。已有历史缺失字段显示未记录，不猜测来源。279 首轮早期打开事件未见弹窗的原因尚未确定，实际按钮重试流程已通过；不宣称整个软件没有其他 UI 时序问题。

未提交、推送、打标签或触发 Actions。
