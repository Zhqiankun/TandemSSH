# 七组服务器监控的真实桌面验收

2026-09-13，F13 / B12。当前本地源码重新 build 并生成 Windows 目录包；未生成公共安装器、提交、推送或触发 Actions。

Windows TandemSSH 实际连接隔离的 Alpine 3.24.1 虚拟机，通过中文服务器身份确认后采集 CPU、内存、磁盘、网络、运行时间、系统信息、进程。完整批次包含 18 个固定命令动作。检查百分比范围、CPU 核数、磁盘列表、eth0 收发计数、进程列表、Alpine 系统信息和正数运行时间；七组中文卡片必须实际出现在页面，离线占位消失。

实际点击“暂停采样”，等待当前批次结束/取消后记录批次列表，等待 11.5 秒（配置周期为 5 秒），确认没有当前采集及新增批次；点击“恢复采样”后出现新的完整 18 动作批次。最终再次暂停，应用正常退出并释放端口。

证据目录：`.cache/desktop-observation-report-68eaaaf8-9aa9-45e6-bb1b-5b084b5653c0`，包括 `metric-values.json`、`monitoring-result.json` 和截图。已查看暂停截图：显示已暂停、每 5 秒、恢复采样按钮、18 条命令的完成记录和 CPU/内存/磁盘数值，下方可见网络/运行时间/系统信息卡片。进程卡片位于当前截图下方，其标题及数据由界面断言与实际 API 数据共同核对。不能将截图描述为全部卡片同时完整可见。

首轮报告 `19e11860-9bb0-44e1-a880-5982143995c8` 只等待后台完成采样，截图仍为界面离线占位，因此不作为卡片显示通过证据。补充等待七组卡片与离线占位消失后，最终报告通过；未为通过断言改动生产界面。中间一次脚本生成使用了错误翻译文件路径，生成失败；随后改为实际 zh_CN.json，最终运行包含卡片断言。

回归：24 文件、195 项测试全部通过，无跳过，包含监控采集器、显式 Linux manifest 的集成测试、固定采集运行层及中文采集面板。日志 `.cache/full-metrics-tests.log`。构建与目录包日志分别为 `.cache/full-metrics-build.log`、`.cache/full-metrics-package.log`。

F13 原始标准“CPU、内存、磁盘、网络、系统和进程查看；采样状态可见”已满足，标记 verified。B12 的七组查看和暂停恢复已满足；主机设置中的 metricsEnabled 开关及自定义 metricsInterval 数字输入已核对代码，但本轮通过 API 创建测试配置，没有实际操作主机编辑界面修改周期、保存和永久停用，所以 B12 保留 not-fully-verified。

边界：本轮证明 Alpine 普通运行环境下的实际采集和桌面显示，不证明所有发行版、真实 OOM、精确负载计量或长期历史聚合。BusyBox 无百分比字段的进程显示“—”，不伪造为 0。暂停的实机断言是无新增采样批次；执行中通道取消及不使用 PTY 的独立证据仍见 51/52，不能将本轮批次断言说成抓取了所有原始 SSH 命令。

清理：桌面验收进程退出码 0；专用虚拟机按准确 manifest 停止，停机工具返回 forced=true，启动器退出码 0。虚拟机为超时后的强制清理，不将其称为操作系统正常关机。
