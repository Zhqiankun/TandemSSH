# 独立 C2S 的准确重连说明

2026-09-14，继续F12/B13。发现C2STunnelPresetManager展示maxRetries/retryInterval输入，但Electron独立C2S主进程没有按这些值调度重连。它们不能作为可用的重试功能向用户展示。

本轮移除独立C2S的两个无效输入，改为按模式显示说明：远程转发断开后检查连接并手工启动；本地/动态不定时重试，监听仍开放时新的客户端连接可创建新转发通道，排查时可停止监听。实际运行逻辑未改变，服务端S2S的有效重试配置保留。旧配置字段未删除，保持存储格式兼容；不声称这些字段在独立C2S生效。

页面只负责行为说明，主进程仍拥有监听及中继生命周期，无新共享抽象或依赖方向。简繁中文、英文及其他语言回退齐全。

新增三模式中文组件测试：准确说明可见、无无效重试输入；与既有取消/端口/权限错误共10项通过。tsc -b、两处文件ESLint、本地化检查通过。日志.cache/c2s-reconnect-ui-tests.log、c2s-reconnect-ui-types.log、c2s-reconnect-ui-lint.log、c2s-reconnect-ui-localization.log。

本轮是澄清实际产品行为，不是新增自动重试实现。尚待打包桌面验证说明及连接断开场景，F12/B13继续未完全验收，整体65/79。未推送Git或触发Actions。
