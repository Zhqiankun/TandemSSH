# 多文件编辑标签导航

2026-09-13，继续 B08/F09。代码审查确认此前 WindowManager 只有叠放的文件/差异/终端窗口，没有可切换的编辑标签。新增同一文件管理器内的编辑标签栏，按窗口标题显示，方向键及 Home/End 切换；当前标签对应最高焦点窗口，隐藏非活动窗口但保持挂载，草稿和异步操作不会因切换而卸载。

增加“文件列表”入口，用户可不关闭编辑文件返回目录继续打开其他文件。列表为标签栏预留36px空间，避免覆盖文件管理器工具栏。重新打开窗口或选择标签退出列表视图。关闭仍通过各 FileWindow 的既有关闭按钮及未保存确认，标签栏没有新增绕过确认的关闭入口。

责任：WindowManager 管理标签和窗口焦点/显示；FileWindow 继续拥有文档、草稿、保存和关闭保护。公开窗口管理接口不变，不新增后端协议或共享抽象。使用 useId 为不同文件管理器实例隔离 tab/panel DOM id，避免跨主机标签定位冲突。

验证：实际窗口管理组件打开两个独立状态编辑子组件，分别输入不同草稿，鼠标/方向键切换保持值，关闭当前后另一份草稿仍保留；与现有窗口几何和 FileWindow 编码测试共3文件5项通过。TypeScript、ESLint通过，缺失翻译键0。日志 .cache/editor-tabs-tests.log、editor-tabs-tsc.log、editor-tabs-lint.log、editor-tabs-locales.log。

本轮尚未重建目录包完成真实SFTP多文件标签与语法高亮验收；多窗口同时平铺展示不再是此标签模式默认行为，窗口仍保持原几何/最大化能力。需要实机检查列表返回、尺寸重算和未保存关闭流程。整体44/79，未推送或发布。
