# 主机搜索支持备注与分组路径

2026-09-13，F01/B01 局部补齐。

原 hostMatchesQuery 仅匹配名称、IP、用户名和标签。现在同时匹配主机 notes 与 folder，使用与既有字段一致的小写子串匹配；缺失字段为空字符串。搜索层不写入主机配置，不修改原目录/子主机展开偏好，不调用连接接口。

新增回归先复现失败：折叠的“生产 / 数据库”下 db-node，备注“夜间备份服务器”，按备注或完整分组路径都无法命中。修改后，两种搜索均展开到目标主机并保留祖先目录；清空搜索后原目录仍折叠，openSet 未被修改。

树可见行、树构造及排序共 3 文件 31 项通过；TypeScript、ESLint 和 diff 格式检查通过。日志 .cache/host-search-before.log / host-search-after.log / host-search-tsc.log / host-search-lint.log。仅修改既有搜索模块与测试，无新共享抽象或依赖变化。

尚未重建目录包进行本轮搜索 UI 实测，主机管理整体验收继续保持未完成，38/79 计数不变。未提交、推送、打标签或触发 Actions。
