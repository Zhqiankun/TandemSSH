# 拒绝导入凭据名称的歧义绑定

2026-09-14，继续A22。发现凭据名称映射按小写键覆盖，Deploy/deploy对应两份凭据时会任意选中后遍历的一份。导入别名指向该名称也会继承错误绑定。

host-bulk-routes.ts的导入期名称映射改为number|null：不同ID竞争同一名称时标记歧义，该标记不能被后续重复名称恢复。导入别名指向歧义名称时传播标记且不新建占位凭据。主机使用歧义别名时拒绝该条并返回HOST_IMPORT_CREDENTIAL_AMBIGUOUS及中文“凭据名称对应多条记录，请明确选择凭据后导入。”。明确数字ID仍走377/378已验证的用户范围查询。

映射仍是单次导入内部状态，不写库、不新增共享模块或跨层依赖。本轮不改变已有凭据，也不迁移旧错误绑定。

真实HTTP/SQLite新增两个场景：名称大小写冲突直接引用、导入别名指向该名称。修复前两项错误成功，修复后均失败，主机列表为空，凭据数量保持两份。导入HTTP与批量路由共23项通过。日志.cache/import-alias-before.log、import-alias-final.log。

尚需合并377—379的打包桌面错误提示验证及其他高级字段验收，A22不标记整体通过。总进度60/79。未推送Git、未触发Actions。

最终tsc -b及两处修改文件ESLint通过，命令链exit0；日志import-alias-types.log、import-alias-lint.log。
