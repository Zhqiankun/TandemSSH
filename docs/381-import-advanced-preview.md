# JSON 导入逐主机高级配置类别预览

2026-09-14，补充A22用户可见预览。原先每台主机只显示名称和地址，高级字段只由一条通用说明提醒。

新增sidebar/host-import-preview.ts，职责仅为导入草稿的展示分类；输入只读记录，输出固定类别键，不输出字段值，不调用API、不写入、不判断安全性。HostsPanel继续编排文件读取、中文确认和提交，不把分类结果用于授权或后端规范化。无新增共享抽象或反向依赖。

每台JSON主机摘要增加九类配置存在性：认证/凭据引用、代理、跳板/父连接、终端/快捷命令、隧道、监控、Docker、Proxmox、端口敲门。明确标注“文件包含的配置（不代表已启用）”，因此显式false或空配置也如实列出。未提供相应字段时显示未提供说明。密码、私钥、代理密码、终端命令值不用于类别摘要。

中文简繁与英文文案已提供，其他语言使用英文回退。新增纯展示测试验证敏感值不进入输出、输入不被修改、false/空对象不被误称缺失、缺失和继承字段不被误报。预览/导入错误适配共2文件5项通过；tsc -b、三处文件ESLint、本地化检查通过。日志.cache/import-preview-tests.log、import-preview-types.log、import-preview-lint.log、import-preview-localization.log。

本轮没有打包桌面验证新摘要；此类别清单不等于逐字段值审阅或覆盖差异，SSH配置摘要未在本轮扩展。A22仍保留高级字段详细审阅、覆盖差异和运行资源影响的剩余范围，总体60/79。未推送Git或触发Actions。
