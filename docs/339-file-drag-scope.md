# 文件拖放绑定起始会话与目录范围

2026-09-13，继续F09/B09。网格内部拖动状态可能跨FileManager的会话/目录变更保留，drop会使用当前传入的操作回调处理旧文件，存在在新范围派发旧操作的风险。

FileManager向FileManagerGrid传入稳定operationScope（会话ID、目录、搜索词和隐藏项显示偏好的JSON值）。拖动开始捕获该范围；drop先核对一致性，不一致时不调用移动/比较回调。范围变化的effect清空拖动状态，避免离开后再返回原范围使旧拖动复活。未提供scope的其他调用方保持兼容。

范围判断属于文件网格输入编排，不替代后端权限和路径校验，没有新增SSH连接、修改批次执行器或发送删除请求。实际已发出的文件操作仍遵守原有“已确认计数、未知结果不自动重试”契约。

真实组件事件测试：会话切换、目录变更两项修改前失败；修复后旧drop不触发操作。同范围正常拖动仍传递原选中项和目标；离开再返回不会恢复旧拖动。与文件批次及sudo待确认测试共3文件15项通过。虚拟滚动仅替换为确定行以供jsdom测量，不模拟拖动处理函数。日志 .cache/grid-drag-scope-before.log、grid-drag-scope-final.log。

尚未重新打包执行系统拖放/真实SFTP移动中断矩阵。整体46/79，未推送或发布。
