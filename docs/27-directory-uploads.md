# 目录上传批次

状态：实施中。目标覆盖目录/多文件上传、空目录、改名/跳过/明确合并覆盖、逐项结果、接管、取消、恢复和记录；不会用文件成功代替整个目录成功。

本轮先建立远端预览和目录写入用例，再接入原生来源选择与现有 UploadQueue。UploadTreeService 拥有远端清单、版本、策略和目录结果；UploadService 继续拥有唯一的分块写入和提交校验。SftpFileIO 只提供非递归 mkdir 等底层原语。HTTP 复用人工身份校验；自动/MCP 传输仍需单独接入相同任务网关。

预览只读，不提前创建目录。已有文件保存完整摘要基线；已有目录需明确合并。路径只由根目录、父 ID 与名称构造，拒绝路径穿越和子目录链接。确认绑定版本，目录创建使用文件用例的接管检查、路径提交锁和前后操作记录；不调用 Shell、sudo 或 chmod 777。新目录请求权限 0700，实际属性与结果一并记录。创建后无法核实的结果保留为 unknown，不能自动重试。

文件准备会绑定批次的服务器、路径、父目录及覆盖基线，再交给现有分块服务。SFTP 缺少可移植的目录 inode/CAS，不承诺对服务器外部进程的原子隔离；同路径同属性目录被替换的剩余竞态需如实保留。

来源侧由原生选择/真实拖拽文件对象建立只读能力，枚举空目录并拒绝跟随链接；读取按块受控。界面只编排预览与队列，不能提供任意本地读取路径。批次文件共用 UploadQueue，并在完成/清除时回收预览，避免大量文件占满旧记录额度。

验收使用真实 SFTP/本地文件，覆盖预览无写入、合并覆盖、目标变化、链接、取消、权限失败、未知结果、API Key/跨用户拒绝、接管及既有上传/协同回归。后续记录区分核心实现、界面接入与实际桌面验证；完整原始目标仍在实施。

## 本轮已实现的核心

新增 types/upload-tree.ts 与 backend/files/upload-tree-service.ts，提供只读目标预览、层级/重名检查、版本确认、目录创建、结果查询、保活、取消与释放。生产入口复用 UploadPorts 的目标解析、beginWrite、共享路径锁和审计。SftpFileIO 增加非递归 mkdir；不引入 Shell/Sudo 路径。

UploadService 增加内部 UploadConstraint，绑定预览中的服务器、路径、已有文件摘要与父目录属性；准备及后续操作重查约束。新增 touch/forget 支持长任务及记录回收，513 次准备/取消/释放后仍可继续使用；释放不会删除最终文件，也不能清除未知或仍有临时文件的记录。

electron/upload-sources.cjs 拥有本地来源选择、受限枚举和块读取；upload-source-ipc.cjs 校验主窗口顶层页面，并在导航/账号重置时撤销能力。preload 使用 Electron webUtils.getPathForFile 转换真实选择的 File 对象，并禁止通用 invoke 直接访问这个保留通道；字符串路径或伪造的 File 不能由该桥接入口获得读取权限。链接和特殊文件排除，读取时核对父目录、文件身份、大小和时间版本。

types/upload-source.ts 和 uploads/native-source.ts 提供异步块来源适配。现有 UploadQueue 继续处理全部分块传输，支持原生异步来源；摘要准备前后及提交前复核来源，空文件同样检查。没有复制第二套文件上传状态机。

真实 SFTP/文件与队列联合回归 15 个文件、120 项通过（.cache/upload-tree-core-final-regression.log），包含目录创建、空目录、明确合并/覆盖、接管、锁冲突、父目录替换、未知结果、HTTP/IPC 身份、来源变化、真实 File 转换边界与既有自动/协同终端。类型和改动模块 lint 通过，中文错误键已补齐。Windows 夹具只模型化 POSIX 所有者属性，不能替代真实 Linux/OpenSSH 权限验收。

## 中文批次界面与队列接入

FileManagerToolbar 新增“上传目录”按钮；目录拖拽和桌面多文件上传进入 UploadTreeDialog。useDragAndDrop 在事件内传递真实 File 对象，preload 再映射为原生来源。旧目录拖拽分支的浏览器递归、提前 mkdir 和忽略创建错误逻辑已移除。手工创建目录仍保留自己的既有入口。

文件责任与依赖：upload-tree-api 只适配受登录限制的 HTTP；UploadTreeDialog 负责来源/目标预览和人工选择；upload-batches 拥有目录步骤、成员结果、账号生命周期、保活和两端记录回收；upload-bindings 定义批次与 UploadQueue 的公开契约。UploadQueue 继续拥有唯一分块上传状态机，业务批次不直接调用 SFTP 或 Shell；原生来源仍经受限 preload/IPC，界面不能传任意本地路径。

预览显示本地来源、远端名称、目标路径和已有大小，支持逐项/整批合并、覆盖、跳过，父目录跳过作用于全部后代。改名后必须重新检查；新预览清除旧冲突选择。接管复选框默认关闭，授权仅用于本批次。关闭未开始的预览释放资源，已入队批次由全局队列继续管理。

队列上限为 4096 行，批次确认前预留容量，账号变化使旧预留失效；整批入队后统一通知，列表每页 100 行。文件继续使用已有并发、暂停、源文件重检和摘要校验。目录有独立结果，不展示文件摘要成功状态。允许重试已知失败的目录，文件需逐项重新尝试；已完成项不重新上传。

完成文件主动释放后端传输记录，清除队列行时重试失败的释放；最后一个可清理成员释放远端批次和本地来源。清除已完成行后仍保留批次累计数。整批取消立即停止剩余文件，并观察目录请求的最终结果；丢失目录响应保留未知结果，取消和清除不能把它变成已取消或成功。全部跳过不接管 AI，也不读取文件块。账号切换撤销本地来源并取消旧请求。

当前会话内暂停/恢复和保活已接入；应用退出后不会声称还能续传。本机来源选择与批次预览仍有容量限制，服务端父目录原子替换的可移植 SFTP 限制保持不变。

## 实际验证与剩余范围

联合回归 **22 文件 / 149 项通过**，日志 .cache/upload-batch-final-regression.log；包含真实本地来源 + SFTP + 批次 + UploadQueue、目录/文件下载、中文预览、原生 IPC、接管和既有自动/协同 PTY。新测试验证二进制/空文件/空目录、无接管授权拒绝与显式重试、全批跳过、来源改变、丢失目录响应后的取消/清理、确认期间切换账号、改名后重新审阅和父级跳过。

完整类型检查通过（.cache/upload-batch-final-types.log）；改动模块 lint 为 0 错误、1 条既有 FileManager 未使用变量警告（.cache/upload-batch-final-lint.log）；中文静态键缺失为 0。前后端构建与本地解包验证包通过（.cache/upload-directory-ui-build.log、.cache/upload-directory-ui-package.log），保留既有大资源块警告；本地验证包复用原生模块，标准原生重编译由 CI 验证。

修正过的失败：测试夹具误把完整原生条目交给严格 HTTP 映射、错误使用 Uint8Array 代替服务端 Buffer；批次通知优化漏传 notify 参数。没有放宽生产 schema 或分块输入校验，修正后完整联合回归无失败或未处理异常。

上一核心提交 20bb3ba 的 CI 34212352302 已完成全部检查、标准 Windows 构建、实际安装、中文启动和卸载保留数据验证。当前界面提交的云端结果及实际桌面场景另行记录。

完整目标继续：AI/MCP 文件传输与文件流程、跨重启检查点、跨目录另存、历史草稿与凭据统一、基础认证/隧道/监控、实际跨版本升级及全量验收。Windows SFTP 夹具不能代替真实 Linux/OpenSSH 权限与网络矩阵。


## 2026-09-08 实际桌面目录上传

当前源码构建的 Windows 解包桌面在独立资料目录启动，完成测试 SSH 指纹确认、“上传目录”入口、本地来源选择、中文冲突预览、同名文件改名后重新检查，以及已有目录明确合并。最终 5 项完成、0 失败、0 未知；远端文件内容、空文件、空目录和原有同名文件保持不变均通过核对。应用正常退出，测试进程和后端/调试端口均已关闭。

证据目录 .cache/desktop-observation-report-ec32ce29-2332-44ec-aff9-281b5c179440，包含 upload-directory-preview.png、upload-directory-completed.png、upload-directory-result.json、desktop.json；执行日志 .cache/upload-directory-desktop-observer.log。测试从 main-process 对原生目录选择器注入专用测试来源，未声称人工操作过原生系统对话框。最终截图已实际检查，预览可滚动，确认和接管控件可见。

首次桌面场景因为夹具不支持 OpenSSH 原子重命名扩展，已有文件覆盖被正确拒绝：4 项成功、1 项失败，原文件保留，失败项显示临时文件与中文原因。失败快照 .cache/desktop-observation-report-c50953a9-ad40-4914-9581-e2174bca8db0/desktop-failure-state.json。后续使用界面改名并重新检查完成上传，没有放宽原子覆盖要求。真实 Linux/OpenSSH 的覆盖成功路径、权限/元数据和网络矩阵仍需后续验收。
