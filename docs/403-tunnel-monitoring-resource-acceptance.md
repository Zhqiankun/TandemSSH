# 隧道与监控资源边界原始验收 A35

2026-09-14，按06-delivery原始A35“端口范围正确、不混入PTY、采样可关闭、资源可释放”逐项核对。本轮没有修改生产代码，不将所有认证/权限组合都追加为A35的门槛。

| 原始要求 | 直接证据 |
| --- | --- |
| 端口范围正确 | 73真实S2S单/双主机三模式及六种冲突；当前矩阵明确断言双主机监听建立在正确一侧；74/76真实Windows↔Alpine三模式数据；398端口1–65535建连前预检；中文表单按模式标注本地/远端端口 |
| 不混入PTY | 当前S2S矩阵远端exec计数0、以SSH转发流逐字节交换二进制；402客户端隧道流程终端会话0；275监控ptyCount0，关闭采样后无新命令 |
| 采样可关闭 | 275真实中文编辑保存metricsEnabled=false，15.5秒无新采样命令；重新打开仍关闭，再启用后恢复采样；B12已有274/275完整指标证据 |
| 资源可释放 | 当前真实S2S停止销毁活动流、无重试计时器、端口重新绑定；74/76三模式端口释放与注销清理；399/400认证后撤权零转发/监听且SSH关闭；401转发拒绝清理；402确定性监听前取消及30次立即停止 |

## 当前回归与原始报告

当前tunnel-trust、C2sSession、真实WebSocket探针、中文取消组件和模式标签共5文件86项通过，无跳过。日志.cache/tunnel-resource-original-scope.log。源码未变，不重跑无必要的类型或构建；当前包为402已构建版本。

本轮复读以下原始JSON：
- 775986ed-c697-4257-8b59-e4362e0e60e5/standalone-c2s-result.json：三模式真实Linux数据、停止端口释放、旧身份拒绝、明确重试和注销。
- b5e770a2-834e-4253-b0ff-cff01d4d12dc/tunnel-conflict-result.json：中文冲突可见且原监听保留。
- 0ec45e43-5c3f-44c6-be65-63ba055d1aab/metrics-settings-result.json：实际停用、观察期无采样、重新启用及PTY为0。
- 65031e36-9057-4958-8774-abf87bd1de8b/c2s-before-listen-result.json：local/dynamic监听前取消、迟到结果不创建SSH/runtime。
路径前缀均为.cache/desktop-observation-report-，环境和清理由原文保留。

端口正确指按用户配置建立在声明的监听侧和目标，默认回环、合法端口；不宣称动态代理具备未设计的任意目标沙箱。未证明持续传输后所有权限变更即时生效，也未覆盖所有Agent/认证插件。F12/B13及整体R10仍独立验收，不能因资源条款通过被一并关闭。

A35标记verified，整体65/79，剩余14条。未推送Git或触发Actions，公开安装包不变。
