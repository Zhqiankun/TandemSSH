# 双向目录冲突的 Windows 桌面验收

2026-09-14，继续 B07/B05/F09。用当前工作树重新构建本地 win-unpacked，包含345的下载启动失败提示；没有生成公开发行资产或触发 Actions。版本字段仍为 alpha.14，不能与公开 alpha.14 的源码范围混同。

## 实际执行

独立 Windows Electron 客户端连接真实 loopback SSH/SFTP。使用新的应用数据目录、临时本机目录和测试服务端目录；原生目录选择器仅替换为返回该次测试目录，文件浏览、预览、确认、队列、IPC、SSH/SFTP 和磁盘写入均走产品实现。

最终报告：.cache/desktop-observation-report-4badf0d8-b2d2-461e-bb0a-b7e1984d9649/tree-conflict-result.json。运行日志 .cache/tree-conflict-overwrite-native-run.log；场景 .cache/tree-conflict-scenario.txt、运行器 .cache/run-tree-conflict.cjs。前一轮基础场景报告为06bddd0c-0ae0-4b0e-947a-c55fff30a50f，所有断言通过。

1. 本机与远端都存在“项目/结果 %.txt”，内容各不相同。上传默认不能开始；点击本批覆盖文件仍不能开始，目录合并没有被自动授权。父目录改名后必须重查，新文件及空目录实际写入“上传改名”，原远端文件不变。
2. 下载相同目录时默认不能开始；本批覆盖文件仍不能自动合并目录。逐项跳过父目录后，队列显示子文件已跳过；远端专有子文件没有落入原本机目录，原文件不变。
3. 再次下载并将父目录改名为“下载改名”，改名后确认禁用，重查后新建路径可确认。实际子文件内容、远端专有文件内容、空目录全部正确，两个原文件仍不变。
4. 再次下载到原目录，单独合并父目录及空目录仍不能开始；逐项明确选择覆盖同名文件后才允许开始。确认前检查原本机内容，确认后实际变为远端内容，新增子文件正确，远端源文件不变。

两轮客户端都报告正常退出并释放后端端口。已人工查看第一轮下载改名预览截图：中文控件、目标子路径和跳过4项的队列摘要与断言一致。

## 剩余范围

此次没有改动产品行为、API契约、模块依赖或共享抽象，提供当前真实桌面的直接验收证据。B07还需补当前桌面上传明确覆盖的成功路径，以及批量/逐项混合策略的剩余组合；不能用此处下载覆盖成功推断上传覆盖成功。当前 SFTP 夹具不支持原子覆盖扩展，应使用真实 Linux OpenSSH 验证对应上传路径。B05完整传输与B06动态并发验收也仍未全部完成。整体保持47/79。
