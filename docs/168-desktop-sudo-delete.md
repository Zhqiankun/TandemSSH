# Windows 桌面 sudo 取消与确认删除

2026-09-12，源码基线 1b2cc8f。本轮从最新源码生成本机 win-unpacked 验证包，连接隔离 Linux OpenSSH，补齐 167 的实际桌面输入与提权删除场景。没有新生产代码或依赖变化。

## 实际流程

专属实例 ab4997c4-b50b-49b3-b9c5-41407ba116af，保持网络 restrict=on；沿用官方 sudo APK 离线安装、默认签名检查和需要密码的 sudoers 配置，经 visudo 验证。

新建独立桌面配置和唯一测试目录。仅在该实例将测试目录父权限设为 root 所有/755，并创建空的不可访问测试回收站目录，使 UI 按既定流程提供永久删除确认。原始文件内容为 fixture。

从文件右键删除进入中文确认；仅针对当前测试阶段的“无法移至回收站”浏览器确认允许继续。普通删除被权限拒绝后出现“需要管理员密码”对话框，点击“取消”，独立 SFTP 核对原文件内容仍为 fixture。重新发起删除、再次明确确认，再通过密码输入框提交该实例的测试密码；独立 SFTP 验证原路径消失。未从 UI API 绕过密码对话框或直接执行目标删除。

## 证据

报告：.cache/desktop-observation-report-4d49c7bf-32ca-4da2-bd4d-34d9c7a4f5f0/sudo-desktop-result.json
截图 sudo-password-dialog.png 已实际查看，中文标题与取消/确认控件可见，截图时密码为空。sudo-delete-completed.png 保存执行后界面。观察器输出 cleanExit=true，进程退出 0。

原路径、文件内容通过独立 SSH/SFTP 连接核对；测试目录属主恢复、空测试回收站目录移除，夹具删除唯一测试目录。测试没有接触业务主机、日常客户端配置或公开安装包。

本轮覆盖正确密码与取消提权；错误密码后的桌面重试、批量部分失败、sudo 过程中连接切换等完整交互仍未全部验收。B09 继续保持未全部验证。该开发目录包虽版本号为 alpha.8，但含标签之后的代码，不等同于公开 alpha.8 安装包。
专属 Linux 实例经 QMP 关闭，启动器退出 0。git diff --check 通过。
