# F14 / B15 设置与数据联合验收

2026-09-14。以01-product.md/F14及09-base-features.md/B15的原始首版条款为依据，逐项核对现有实现和直接证据。没有修改生产代码。

## 逐项证据

| 原始要求 | 当前实现与实际证据 |
| --- | --- |
| 本地配置备份/恢复、导入预览 | ConfigurationBackupPanel、schema/service/repository：完整规范化预览、显式确认、追加主机、无凭据恢复、冲突检查、SQLite事务与持久化收据。54/55基础及外观；71网络与预设；72终端默认值和配色库；424/425编码；461/462管理员创建默认值；463—468本地C2S与跨重启接续。 |
| 界面偏好与布局 | desktop-configuration及共享显示/布局契约。458/459侧栏；472/473实际自定义卡片顺序、面板、宽度和默认页恢复及冷启动。147项迁移回归包含旧备份不覆盖、显式默认、失败回滚。 |
| 快捷键设置 | 当前配置与键盘派发实现，55及459/473真实导入：新ID、禁用待审阅、旧片段引用不误绑定，手工编辑一项只启用该项；现有快捷键匹配/派发回归。 |
| 主题/编码设置 | 72配色库追加新身份、同名改名、主机22px/配色可见；425四种编码恢复后经真实SSH收发；当前编码适配回归。 |
| 日志位置与清理、留存 | 470当前用户目录/占用/7天及100MiB规则、显式确认清理，与写队列串行。471真实桌面取消保留、确认仅清过期、新日志及普通文件保留、统计刷新；容量及路径重定向拒绝由文件测试覆盖。 |
| 脱敏日志导出 | AuditHistoryReader、流式HTTP及完整性校验、HistoryExport。77范围及流异常；471当前包真实下载、record/summary完整、测试password不在导出中；当前客户端流/取消测试。 |

## 本轮直接检查

重新读取71最终network-backup-result及restart原始报告24f1ae05…，72终端备份及restart报告a8c523d3…，425四份*-backup.json；全部与表中范围一致。当前代码的桌面布局契约、备份字段、原始条款也重新核对。462/467/468/471/473近轮直接报告和可见截图记录补足原索引中实例默认值、本地C2S、日志维护与真实布局缺口。

联合回归15文件240项全部通过，补齐导出客户端和编码3文件11项通过，共18文件251项，无跳过：
.cache/settings-data-acceptance-tests.log
.cache/settings-data-export-encoding-tests.log

测试覆盖字段校验、引用、权限、选项冻结、幂等、故障回滚、本地文件恢复、UI偏好/键盘派发、日志留存与导出。测试名称本身不作为桌面证据；桌面事实由上表对应原始报告及截图提供。并未重新跑一遍所有历史桌面场景。

## 数据与行为边界

验收对象是明确的无凭据配置备份功能，不是整机克隆。密码/私钥/API Key、MCP配对、指纹信任、活动任务授权和自动执行状态不随普通备份恢复，界面明确说明并要求重新配置/审阅。源机器外部资源与云同步不据此宣称可迁移；09-base-features将云账号/同步等另行规划。流程本身的secret-ref执行能力仍由F11/B14/R09继续验收，不因配置文件能迁移流程而视为已经交付。

日志清理按留存规则，不是恢复已删除历史；导出缺失/损坏提示及上限保留，不能声称所有存储故障下均完整。自定义布局和编码有指定场景直接测试，不声称每个主题/字体/窗口尺寸组合穷举。

## 结论

F14与B15标记verified，整体70/79更新为72/79。之前缺口已有实际实现和独立证据，不是只删除“其余设置”的剩余说明。R10仍依赖B14，其他发布、许可证、流程秘密参数及命令识别边界保留，完整目标未完成。

未推送Git或触发Actions，公开安装包未更新。
