# 当前版本规则管理实机验收

2026-09-13，F07。重新 build 当前代码并生成 Windows 目录包（包含 281 试算代次修复），连接专用 Alpine 3.24.1 VM，通过中文界面完成下列操作。

1. 打开“协作执行→命令黑白名单”，将全局规则改为拒绝 touch，只试算 touch <测试根目录>/trial-must-not-run。显示规则拒绝；后端保存策略不变；独立 SFTP 查询确认没有创建文件。
2. 创建一条真实协作任务 touch <根目录>/old-approval-must-not-run，授权后等待单次审批。规则界面明确保存后，任务转 paused-human、控制权归人工；用保存前 operationId/digest/revision 提交批准，真实 HTTP 返回 400 STALE_APPROVAL，SFTP 确认文件不存在。随后取消测试任务。
3. 全局修改保存为版本 2，关闭重开仍为 touch，试算仍拒绝；未勾选确认时保存按钮禁用，重开不沿用旧确认。
4. 界面新增分组 tag:fixture-policy、当前服务器、任务范围，各保存 touch 拒绝规则，版本 3；关闭重开逐一核对范围与规则。移除任务范围后保存版本 4，重开保持三范围。配置用的任务范围 ID 不代表创建了同名真实任务。
5. 在全局范围添加文件规则：测试根目录下“配置%2F”、目录及子路径、只写、拒绝，启用严格文件白名单。保存版本 5；后端读取 path/kind/access/effect/strictFileAllowlist 与输入一致。重开中文编辑器仍保持这些字段，命令规则未被替换。

成功报告 `.cache/desktop-observation-report-4d7d3880-3870-45c0-9cda-1f0a9ebc6aae/policy-desktop-result.json`。已查看 policy-file-reopened.png：中文三范围、文件规则拒绝、严格文件白名单、目录匹配和只写选择可见；路径输入在截图中被控件宽度截断，完整路径由 DOM 值和后端读取验证，不宣称截图完整显示路径。应用正常退出并释放端口；fixture 删除自身测试根目录。

F07 原文“命令允许/拒绝/确认、服务器范围、文件路径范围、规则试算”验收归并：命令允许/确认/拒绝及作用域优先级由 137/138/282 的真实求值器和网关/中文组件验证；四类范围和文件规则编辑保存、只试算无执行、旧批准失效由本轮当前包实测；存储故障/并发版本控制与实际加密跨进程读回见 139/140。281 的迟到试算作废由组件异步回归验证，本轮包包含修改，但未另行注入网络延迟模拟该竞态。

据原始功能标准将 F07 标记 verified，总计 38/79。规则试算与名称匹配不构成远端操作系统沙箱；符号链接执行边界仍由实际文件网关检查。文件传输功能本身的完整验收继续归对应 B/F 项，不被规则管理完成所替代。

构建日志 .cache/policy-current-build.log / policy-current-package.log，运行日志 .cache/policy-current-desktop.log。未提交、推送、打标签或触发 Actions。

清理结果：桌面观察器退出码 0，专用 VM 按准确 manifest 停止，返回 forced=true，启动器退出码 0；VM 强制清理不记为操作系统正常关机。
