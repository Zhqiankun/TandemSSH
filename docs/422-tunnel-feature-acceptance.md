# 当前整包隧道验收 F12 / B13

2026-09-14。按01-product的F12及09-base-features的B13与第8节核对：本地/远程/动态转发、范围限制、启停、连接状态、端口占用、重连说明及有界资源。不是仅凭一组绿灯宣布全部产品完成。

## 原始范围对照

| 条款 | 直接证据 |
| --- | --- |
| 三种转发 | 73真实单/双主机S2S六模式二进制与SOCKS数据；74/76真实Windows↔Alpine OpenSSH的C2S三模式数据和停止；当前后端矩阵持续回归 |
| 独立启停与身份范围 | 49来源/端点按发起用户授权及主机信任；74本机中继固定、旧身份拒绝；399/400查询/握手期间撤权阻断；402/405真实中文取消、停止、注销及明确重试 |
| 监听与目标范围 | 默认回环；73矩阵断言双主机监听建立在正确SSH侧；398端口1–65535建连前校验；GUI明确选择监听和目标，AI任务目录不提供静默创建隧道的工具 |
| 状态与端口占用 | 73及本轮真实桌面CONNECTED/停止、冲突FAILED与中文原因，原占用服务仍正常；404/405区分独立C2S实际重连方式，406 S2S新SSH实例且恢复数据，不只看SSH连接状态 |
| 取消与失败释放 | 397—402早期查询/监听前/认证等待取消；401转发拒绝清理；421 DNS准备失败无迟到计时器或重试；本轮重测完整C2S取消组合 |
| 资源边界 | 407—414双向流控制与1MiB消息限值；415后端WS128/TCP256；416远程32子流；417桌面32活动/在途名称、同名8请求、每监听32客户端；419每S2S隧道32数据连接；420 S2S32活动/在途/重试名称及同名8启动 |

## 本轮当前包

源码build和electron-builder目录包成功，包含421之前全部生产变更；日志.cache/tunnel-combined-build.log、tunnel-combined-package.log。

S2S当前桌面：报告3cbf68c7-884f-477e-bcb0-4f654de30ed7中tunnel-result.json与tunnel-conflict-result.json已读取：信任前零认证、真实TCP回显、停止释放、中文冲突及原监听保留。日志s2s-current-desktop.log。

C2S当前桌面：报告d5585c0a-776b-4b7e-998c-df1867b5034b中c2s-pending-capacity-result.json与c2s-large-echo-result.json已读取：32真实并发在途请求/第33拒绝/取消后复用/所有端口释放，无SSH被提前创建；8,388,628字节双向回显完全一致。完整取消/注销/说明切换流程同时通过，日志c2s-combined-current.log。

上述报告目录均为.cache/desktop-observation-report-<ID>/。两个客户端cleanExit=true、脚本exit0，固定SSH/Echo端点和业务端口均关闭。近期当前源码后端全隧道/准入71项及各容量/流控制专项证据由398—421保留，不将旧次数累加成新的总测试量。

## 限制与验收判定

资源限额不是全进程RAM上限或任意网络攻击防护。未证明所有第三方SSH/Agent产品、所有多跳认证排列，或持续传输期间每一种外部权限变化都即时断流；不声称独立C2S有未实现的定时重试。以上限制保留，不能借此宣称相关能力已实现。

F12/B13原始条款已具备实现、当前回归与真实桌面/协议证据，标记verified。A35此前独立完成；R10的终端、设置等其余原始范围不被一并关闭。整体67/79，剩余12条。未推送Git或触发Actions，公开安装包仍未更新。
