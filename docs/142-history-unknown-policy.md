# 历史中的未知策略结论

2026-09-12。CommandDecision 已允许 unknown（例如非严格模式下不透明 Shell/解释器请求需人工审查），但 AuditHistoryItem 与 history-reader 只接收 allow/confirm/deny，导致合法历史判断被丢弃。

现在 DTO 与历史投影保留 unknown，中文摘要显示“策略判断: 无法自动判定，需人工审查”。没有修改执行策略或批准条件；旧记录缺失判断保持未记录，非法任意字符串仍被投影过滤。既有磁盘记录不重写，导出行为保持原记录语义。

审计 JSONL 实际写入/查询、中文摘要和导出 HTTP 三文件22项通过；进一步断言三类测试记录都实际存在后，audit-journal16项复测通过。ESLint、中文缺失键和 tsc -b 检查通过。新增回归分别覆盖合法unknown、缺失、非法值，避免以遗漏整条记录冒充字段兼容。

依赖方向：共享历史DTO仅增加合法枚举，后端读投影负责校验，前端既有只读摘要负责汉化。不引入新共享抽象。R05/F08各实际来源入口的完整记录矩阵仍需继续核对，本修复不意味着所有执行时间线已完成。
