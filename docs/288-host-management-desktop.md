# 主机新增、编辑、分组、搜索和删除的桌面验收

2026-09-13，F01/B01，包含 286/287 搜索修复的当前 Windows 目录包。

通过真实中文界面点击“添加主机”，填写本地测试地址、友好名称和中文备注，选择置顶，在文件夹选择器中新建“生产 / 数据库”，设置 SSH 端口 49999 并保存。后端读取确认名称、分组、备注、pin 保存正确。此轮不打开远端连接；SSH 登录与复制证据见 285。

随后从搜索结果点击“编辑主机”，修改名称和备注并保存，后端确认原分组与置顶保留。用中文备注和完整分组路径搜索均能找到主机。

子树准备通过测试 API 添加两级子主机，不将其称为 UI 创建子主机。实际搜索“唯一子主机备注”时，界面同时显示生产/数据库目录、改名后的父主机、中间虚拟机和子数据库。已查看成功截图 host-nested-search.png，层级及搜索框内容清晰可见。搜索无关文本时主机行为空。

从父主机菜单进入删除，先点“取消”，后端确认记录仍在；再次选择删除并确认，后端确认父主机不存在，子主机仍存在。未用直接 DELETE API 代替实际确认按钮。

成功报告 `.cache/desktop-observation-report-bc01480f-667c-4895-93fb-10b84b7eb71b/host-management-result.json`，应用正常退出并释放端口，观察器退出码 0。首轮 0942ad76-6cda-4f02-b482-595b02b95b80 在删除菜单阶段超时，脚本只发 DOM click 未打开下拉菜单；改为真实鼠标按下/抬起打开菜单后整个流程通过。生产代码未因该脚本问题改动。

本轮完成新增/编辑/备注/分组归属/置顶/搜索/删除的实际 UI 验证。最近连接和导入导出证据仍需归并，F01/B01 暂不整体标记完成，计数保持 38/79。构建日志 .cache/host-management-build.log / host-management-package.log，运行日志 .cache/host-management-desktop.log。未提交、推送、打标签或触发 Actions。
