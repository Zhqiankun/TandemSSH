# 按原始范围归并导入验收 A22

2026-09-14。重新核对01-product、05-security-policy、06-delivery、08-workflows，而非从已完成代码反向定义成功。06原始A22为“恶意流程/连接/规则导入：只预览、不自动执行、不自动放宽权限”；05要求预览、验证、无自动执行、策略独立确认。

193/196等后续进展记录提出逐字段覆盖差异、后端预览令牌和更多资源观察。这些是有价值的后续改进，不是原始A22追加验收条款。本轮保留它们为限制，不继续让同一条验收无限扩展。F14/B15的配置备份及其他原始要求仍独立保留。

## 原条款证据

| 范围 | 当前实现与直接证据 |
| --- | --- |
| 流程预览、不自动执行 | 191：导入完整定义供编辑审阅，schema拒绝隐藏脚本字段；明确保存仅存定义，绑定主机后才可预览，开始仍需任务授权；本轮重跑库与中文组件 |
| 配置恢复不扩权 | 192：真实恢复服务/SQLite预览不写入，忽略policy/rules/runAfterImport，原严格策略逐字不变，主机未配置认证且自动启动停用；本轮重跑 |
| 旧主机文件预览确认 | 196/380/382/384/386：真实JSON与SSH文件输入、原生中文确认；取消不写入；覆盖模式明确，类别摘要不含秘密值，正常/失败/跳过结果与实际存储一致 |
| 导入不激活后台或隐藏命令 | 194/195/382：采样、隧道、Docker、Proxmox/Tmux后台开关停用、autoStart=false、无新终端；199实际SSH字节捕获证明旧启动片段、环境变量和Mosh输入关闭，人工仍可输入；当前Terminal相同分支仅记录中文说明 |
| 不通过旧入口替换策略 | 当前database.ts在桌面模式提前拒绝/database/import，要求配置预览；原始规则文件不作为受支持导入格式，不能声称独立规则导入已实现 |
| 旧JSON夹带策略/执行声明 | 本轮新增真实HTTP/SQLite用例：顶层与主机policy/rules/allowedHostIds/autoConnect/runAfterImport不能覆盖已有黑名单、不能落入主机记录；隧道停用 |
| 覆盖与运行状态不被误称自动执行 | 377—386拒绝歧义目标/凭据，跳过保留旧记录；387分组变更停止旧授权后续；390/391已有人工流保持原身份，新连接重新读取保存代理/密码，导入本身未自动重连 |

## 本轮验证

当前流程库/中文组件、配置schema/service/SQLite仓储、旧主机HTTP/停用共6文件82项通过；tsc -b、修改测试文件ESLint通过。日志.cache/import-original-scope-final.log、import-original-scope-types.log、import-original-scope-lint.log。首次新增用例用query执行INSERT导致夹具失败，改为既有run写入接口后通过，产品代码未为该测试更改。

复读199原始报告ebb6e4f1-9fa7-409a-a052-fe05e614112b，automaticStartupBytesObserved=false、manualInputObserved=true；复读382/386真实报告9184cce2-6190-47d5-a935-ffe9553d5c4a及bafc740a-281e-4107-a2ef-c6d83941d1e1。其他证据路径由对应记录保留。

## 限制保留

逐字段覆盖差异和旧导入预览令牌尚未实现；没有跨请求导入事务/唯一约束；不承诺更改任意保存字段立即中止既有人工通道，也不把密码案例推广到所有认证和隧道生命周期。它们不能被标为已完成。现有导入是明确确认后保存配置，策略仍需独立编辑确认，执行仍需独立授权。

按原始A22范围标记verified，整体61/79。其余18条原始要求继续推进。未推送Git或触发Actions；公开安装包不变。
