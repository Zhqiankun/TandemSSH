# A37 原始凭据迁移与身份边界验收

2026-09-14。直接核对06-delivery.md/A37：“备份恢复与本地终端——凭据迁移边界清楚，本地/远端身份明确”。

## 凭据迁移边界

当前中文备份说明明确排除密码、私钥、API Key、指纹信任和任务授权；Repository恢复主机始终使用unconfigured，不能继承源认证。461/462补齐管理员创建默认值：密码和数字凭据ID不迁移，恢复清除目标旧认证、停用代理并提示重配。463校验C2S已保存来源身份，464—468对本地隧道清除源审阅和会话状态、保持禁用，并提示重新选择本机SSH主机。

本轮直接读取468普通导入原始结果和查看实际截图，确认新主机未认证、中文重配提示可见、旧身份未继承，冷启动继续保留；467有数据库先提交而本地未写入时实际退出后恢复的直接证据。二者均为零自动SSH连接。不是仅根据测试名称推断。

## 本地与远端身份

当前LocalTerminal.tsx常驻localBadge“本机终端”，展示主进程返回的真实Shell路径与起始目录，手工显式启动/重启。远端TerminalApp/Terminal使用主机配置标识目标；本地终端不套用远端SSH会话身份。180/235中的真实Windows原生PTY报告08634703…/local-terminal-result.json本轮已直接读取：PowerShell/CMD、实际工作目录、本机标识及无效目录拒绝均通过。当前代码仍保留这些路径。

本轮相关9文件121项回归通过，无跳过，日志.cache/a37-boundary-audit-tests.log；涵盖本机终端选项/目录及凭据迁移、预览、权限、恢复和界面提示。没有将“重读既有原生报告”冒称新跑一次PTY；当前包的新桌面证据为468。

## 结论与限制

按A37原始范围标记verified。原索引把全部设置迁移未完成说明复制到A37，混淆了“边界清楚”与F14/B15的整体功能范围；此次分别核对后修正，不删除F14/B15要求。

不承诺跨机器透明迁移凭据，也不把全部设置、所有Shell/操作系统组合或整项目宣布完成。F14/B15继续按自己的范围核对日志留存、清理、导出及设置覆盖。整体从69/79变为70/79。
