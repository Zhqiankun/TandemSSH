# 密码与带口令私钥的桌面认证验收

2026-09-13，A01，承接 294/295。当前 Windows 目录包连接隔离 ssh2 协议服务器；服务端真实校验密码和 RSA 签名，记录认证请求数，并提供会话标记。不是 Linux shell 执行测试；真实 Linux 密码/私钥连接另见 285。

四场景均通过实际主机终端按钮发起：

- 正确密码：服务器校验通过，桌面收到真实 SSH 会话标记。
- 错误密码：认证拒绝，显示中文提示；失败后等待 25 秒，SSH 认证请求与终端 WebSocket 次数均不增加。
- 正确加密私钥口令：AES-256-CBC 加密 PEM 解密成功，服务器验证 RSA 签名通过，桌面收到会话标记。
- 错误私钥口令：连接准备阶段本地拒绝，服务端认证请求数为 0；25 秒内无新终端 WebSocket，中文提示明确不会自动重试。

可进入握手的前三场景均先显示独立计算的指纹确认，确认前认证数为 0。计数中的多次 authCalls 包含 SSH 方法协商，不等同于多次连接重试；停止重试断言比较错误已呈现后的观察窗口。

真实桌面页面最终文本及应用进程日志均检查不包含测试密码、口令和私钥正文标识。服务使用固定测试秘密，不访问个人 SSH agent 或用户服务器。应用正常退出，测试服务器及端口清理完成。

首轮报告 3f9fc3d6-33b8-4b64-9433-29598f50b13f 的认证/计数通过，但截图发现 WebSocket 正常关闭将主提示覆盖成“服务器拒绝了连接”，具体私钥错误仅留在日志。修复 Terminal.onclose：已明确禁止重连且已记录错误时保留该错误，不再覆盖为通用关闭原因。13 项分类/协议回归与 build 通过，再打包重跑全部四场景。

最终报告 `.cache/desktop-observation-report-9d556888-0478-43d9-b99f-5100fbe0a297/auth-matrix-result.json`，运行日志 `.cache/auth-matrix-desktop.log` 含 logSecretsAbsent=true。已查看 key-bad.png，主区域与日志均保留“无法加载 SSH 私钥，请检查私钥格式和口令。不会自动重试。”，没有通用服务器拒绝覆盖。

A01 原文“正确认证能连接，失败不泄露秘密、不循环尝试”由本轮真实桌面/SSH 协议、294/295 本地准备与兼容回归、285 的 Linux 正确连接证据覆盖，标记 verified，总计 42/79。F02/B02 的 agent、交互认证、跳板与重连完整矩阵仍继续验收，不能将 A01 通过扩大为全部连接功能完成。

构建日志 .cache/auth-matrix-build.log / auth-matrix-package.log；错误关闭回归 .cache/auth-close-tests.log / auth-close-lint.log。无新公共 API 或共享模块。未提交、推送、打标签或触发 Actions。
