# A27：桌面与真实 Linux 的父流程接管闭环

## 原始验收标准

A27：流程执行时人工输入或父 AI 派发，人工先接管，父 AI 等待，不会混入两套命令。此结论只关闭 A27，不关闭所有工作流、文件、重连或 UI 验收项。

## 当前源码及环境

从当前本地源码重新 build 并打包 win-unpacked，包含第 245 号脱敏修复；版本号仍为 alpha.14，不等同于 GitHub 公共 alpha.14。只构建本地目录包，未提交、推送、打标签或发布。

本轮专用 Alpine Linux 3.24.1 实例 2a81ea71-25fb-4098-aabd-794408cee9bb。通过实际 MCP stdio 配对和 38 工具目录打开真实 SSH，并在中文 Windows 客户端核对指纹、授权、接管、输入、选择恢复决策、逐条审批。模型为本机受控 HTTP 服务，不使用收费模型。

## 真实四组合

报告目录：.cache/desktop-observation-report-fc933188-5c83-433c-82d9-a60506e9bf29。

流程固定三步：printf 独立首步标记、sleep 10、pwd。确认第二步状态为 running 后从界面立即接管，使用真实 xterm Ctrl+C 及手工 printf 恢复提示符，并通过 MCP read_terminal 读到只会在实际执行后生成的完整标记。

| 模式 | 人工决策 | 最终状态 | 操作记录 |
|---|---|---|---|
| 自动 | 跳过 | completed-with-errors | 成功、未知且人工跳过、成功 |
| 自动 | 重试 | completed | 成功、未知且人工重试、新重试成功、成功 |
| 协作 | 跳过 | completed-with-errors | 成功、未知且人工跳过、成功 |
| 协作 | 重试 | completed | 成功、未知且人工重试、新重试成功、成功 |

四种组合的原流程 ID 都不变；printf 首步只有一条成功记录；未知 sleep 记录不被覆盖。协作模式在恢复后也逐条点击批准，模型调用在人工持有控制权期间不增加。每种模式/决策恰好五轮模型请求：初始说明、预览、运行、查询原流程、完成，未重新发起 run_workflow。

最终 parent-matrix-result.json 与四份 task-*.json 已逐项核对；model-checks.jsonl 保存工具顺序。Windows 客户端正常退出，后台监听端口释放。

已查看协作重试截图，能看到原“结果未知”和“已由人工核对并允许继续”，随后两条成功操作；无弹窗遮挡。自动跳过截图因滚动位置未显示完整终态，其终态以实际 API 记录为证，不夸大截图证明范围。终端包装回显仍可见，属于第 246 号未完成的显示改进。

## 与既有测试组合覆盖

- 第 242 号 AiTaskCoordinator + TaskRuntime + WorkflowLibrary + SessionControl 四组合：直接 humanInput 触发接管，未给出处理未知结果的明确决策时拒绝恢复；协作恢复继续等批准；父 AI 同批的旁路命令返回 WORKFLOW_RESULT_REVIEW_REQUIRED。
- parent-workflow.test.ts：活动流程内插入其他命令被拒绝，接管后旧审批失效，第二步中断不重跑成功首步。
- 本轮真实桌面 + Linux：补足实际执行、用户接管/审批入口、人工终端输出、同一流程续跑和真实退出。

三类证据分别检查调度竞争、直接人工输入和实际用户操作路径，共同满足 A27 的原始范围；不以模型提示词代替运行时互斥保证。

本轮生产逻辑未另行改变；新增内容为受控缓存验收脚本、结果记录和验收索引，模块依赖未变化。完整项目目标继续保留。
