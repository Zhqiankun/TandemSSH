# A34：删除、权限与链接作用目标归并验收

2026-09-13。

原始 A34：文件删除/权限/符号链接，明确作用目标，拒绝路径逃逸和静默提权。

## 最新桌面与 Linux

从当前源码重新 build、打包本地 win-unpacked，包含 262—264 的链接属性、权限目标和超时修复。报告 .cache/desktop-observation-report-bee139b8-cb3c-4085-b384-9a75b0e5ab9d。

通过实际 Windows 中文属性对话框与独立 Linux OpenSSH/SFTP 读回验证：

- 链接自身权限位与保存按钮只读，中文说明明确区分链接与目标；所有者/组保存仍可用。
- 链接 GID 改为 1600，目标文件 uid/gid/mode 均保持原值。
- 请求链接 UID 0 被拒绝，中文错误保留；链接 uid 仍 1000、gid 仍 1600，没有提权流程或自动重试。
- 关闭链接属性、明确打开目标文件属性后，将目标 mode 改为 0600 并重新打开核对。

link-properties-result.json 已读取；link-attributes-readonly.png 已实际查看，说明和禁用保存按钮可见。客户端正常退出、后台端口释放，脚本清理自身 UUID 远端目录。

## 当前代码复核

本轮使用同一专用 Linux 实例重新运行 trash-links-acceptance、symlink-acceptance、permissions-acceptance 与 file-gateway：4 文件、22 项通过、无跳过。报告 .cache/a34-boundary-results.json。

- 真实相对链接回收站恢复、永久删除仅移除链接，目标 keep.txt 内容不变；断链仍能列出和删除。
- 真实 HTTP/SFTP 链接解析保留中文/百分号/换行，拒绝断链、循环和特殊对象。
- 真实权限文件/目录正常七次 mode 读回一致；最终文件/目录链接含尾斜杠拒绝，目标 mode 不变。
- 网关通过受控执行端口核对授权范围、后续规范路径、人工接管与重复计费；越界路径没有进入实际读写。这部分是网关边界测试，不能表述成全部真实远端路径攻击测试。

## 提权边界的既有原始证据

本轮直接读取并核对：

- 168 的 sudo-desktop-result.json（4d49c7bf-32ca-4da2-bd4d-34d9c7a4f5f0）：永久删除明确确认；取消中文 sudo 对话框后文件保留；明确提交密码后才删除受限文件。
- 170 的 sudo-retry-desktop-result.json（009d1f83-b55e-42db-8ae8-3405dab6ef2c）：错误密码后文件保留，中文提示要求核对，再次明确发起后正确密码成功。
- 94/179：真实 SFTP 删除链接及断线后回收站恢复；相关删除服务本轮未修改。

## 结论与边界

上述证据对应 A34 原始作用目标、应用级路径范围和显式提权要求，标记 verified。不宣称跨任意外部进程的原子 no-follow：权限最终链接检查与 chmod 之间的竞争限制见 263；人为明确选择目标路径不属于未授权越界。其他 B09/B10 完整功能矩阵仍由各自验收项管理。

本轮生产逻辑未再次变动；新增隔离验收脚本与证据归档，无新增共享抽象或依赖。仅本地构建，没有提交、推送或发布。
