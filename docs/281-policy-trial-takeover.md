# 接管后作废未完成的规则试算

2026-09-13，F07 局部修复。

发现：规则试算尚未返回时，“立即接管”可以正常使用并清除当前结果，但先前请求返回后仍会 setTrial，重新显示接管前的结论。新增中文界面测试先复现 1 项失败。

修复仅在 PolicySettings.tsx：用组件内 trialGeneration 识别当前试算；清除试算、修改草稿/命令/目录、保存以及发起接管时使旧代次失效。请求完成时仅当前代次可以显示结果。接管发起即失效，不等待接管网络响应；接管后重新试算正常工作。未修改远端执行、策略组合、保存 API 或鉴权，没有新增共享模块与依赖。

最终 3 文件 22 项测试通过：中文规则编辑器、策略 schema 与持久化。新用例验证迟到结果不重新出现、未调用保存、之后重新试算成功；原有保存进行中仍可接管的测试保持通过。TypeScript、ESLint、diff 格式检查通过。日志 .cache/policy-trial-before.log / policy-trial-final.log / policy-trial-tsc.log / policy-trial-lint.log。

边界：这是试算显示的竞态修复，不是规则执行被绕过；原服务端执行控制未变。当前目录包尚未包含本轮 UI 修改，完整规则组合和实际桌面保存/试算验收仍待推进。F07 不标记整体完成，计数保持 37/79。未提交、推送、打标签或触发 Actions。
