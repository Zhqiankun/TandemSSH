# 浮动文件编辑器可见边界

2026-09-11，Windows 链接验收的截图发现浮动编辑器超出文件工作区，右侧和底部控件被裁剪。默认初始坐标和 900×660 尺寸没有在挂载时受容器约束；已有约束只在拖动、缩放时执行。

责任：DraggableWindow 拥有浮动窗口的可见性；同目录 window-geometry.ts 只计算该窗口在定位容器内的位置和尺寸，不成为跨业务通用工具。通过 offsetParent 的尺寸确定坐标边界，ResizeObserver/窗口 resize 更新容器尺寸，布局阶段在初次显示、拖动缩放结果、还原最大化和容器变化时夹紧窗口。正常尺寸保留既有位置与顶部操作区，小容器以控件可见优先于最小尺寸。没有后端、数据或文件内容修改。

5 项测试通过（2 文件），包括实测溢出尺寸、既有位置保留、小容器、隐藏/重复计算稳定，以及 React 组件初始纠正、容器缩小和中文关闭操作。类型和修改文件 ESLint 通过；证据 .cache/window-bounds-tests.log、.cache/window-bounds-types.log、.cache/window-bounds-lint.log。

真实 Windows 后续复测已通过，结果见下节。修复在 alpha.3 标签之后，不包含于正在构建的 alpha.3。

## Windows 实机复测通过

重新构建当前开发目录包，连接自有 Alpine/OpenSSH，实际鼠标双击文件链接打开编辑器。分别在初始、缩小主窗口、还原并拖动到右下角后测量编辑器四边，均位于定位容器与视口交集内；关闭按钮中心 elementFromPoint 命中按钮/其子元素，随后使用真实 CDP 鼠标按下/释放关闭，编辑器消失。无 DOM button.click 替代该关闭验证。

视口从 1184×761 缩小到 984×641，编辑器从 [331,100.75,1184,760.75] 调整为 [331,92.75,984,640.75]；还原拖动后为 [531,212.75,1184,760.75]。三次 fits/closeVisible 均 true。缩小窗口截图已查看，内容、工具栏与关闭按钮完整可见。记录 .cache/desktop-observation-report-329c9de6-9388-4c3b-a98b-e3e484cd1f65/window-bounds-result.json。链接拒绝和目录跟随也在同次观测通过，客户端正常退出。VM 6945c7cb-4a72-4f3d-82a0-1595c3aa2570 正常关闭。

B10 原始条目逐项证据：属性与 Unix mode 查看/修改见 80/84；链接识别、主动跟随与拒绝特殊目标见 81 及本轮；所有者/组修改的远端权限、合法组变更与无权 UID/GID 拒绝见 84。按该原文可标记开发代码验收通过；不外推全部 Unix 发行版或其他文件工作台条目。该窗口修复尚未包含在已公开 alpha.3。
