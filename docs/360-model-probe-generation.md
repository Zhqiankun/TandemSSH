# 新增模型提供方的探测结果绑定

2026-09-14，继续F05。AiProviderSettings新增表单的模型列表自动探测存在异步串结果：修改URL后，旧请求迟到可覆盖新模型列表/警告；旧接口自动选中的模型也会被当前值非空逻辑沿用到新接口。

新增表单内使用请求代次绑定结果，接口类型、URL、Key或表单开关变化时使旧探测失效并清除旧列表/警告。请求自身也递增代次，只有最新结果能更新列表、警告和加载状态。自动建议在配置变化时重置并采用新结果；实际选择或输入模型标记为用户编辑，之后探测不覆盖手工模型，手工输入保持文本模式。

责任仍在模型设置表单，只编排输入和探测状态；probeAiModels公开API、后端网络适配、凭据保存及模型任务预算未修改。没有新增共享抽象或跨模块依赖。旧HTTP请求仍由已有网络生命周期处理，本改动只禁止旧结果回填，不声称取消了所有旧网络请求。

两项问题回归修改前均失败。修复后共9项通过，包含旧列表迟到、新接口自动建议、手工模型保留、旧请求失败不污染新成功结果，以及原有保存和Key存储选择回归。TypeScript与ESLint通过。effect清理处明确说明该ref是请求代次而非DOM引用，对相应DOM-ref提示作定向规则说明，没有关闭其他检查。

日志.cache/model-probe-before.log、model-probe-after.log、model-probe-final.log、model-probe-tsc.log、model-probe-final-lint.log。最后一次修改仅为注释及定向lint说明，不改变测试行为。

本轮尚未重建目录包验证真实表单输入/探测，F05不标为完成，整体54/79。现有编辑提供方表单的保存后模型列表流程不属于本次新增表单修复；后续仍需按F05完整原条款核对。未推送Git或触发Actions。
