# B09 文件与目录操作验收归并

2026-09-14。原始要求：新建文件/目录、重命名、移动、复制、删除；批量操作与部分失败说明。逐项归并真实结果后标记verified，不扩大为整个文件工作台完成。

## 最新恢复流程

报告 .cache/desktop-observation-report-cb6349e6-1577-434d-8433-c95a3ec6d3e2：沿用342真实第二项提交后SSH关闭的故障，确认批次停在1/3。通过界面刷新，服务端接受连接数增加，列表反映远端已完成的前两项；旧批次没有重放。只选择第三项再次拖放，明确完成后Ctrl+Z仅恢复这次第三项，目标中的第一、第二项内容不变。

恢复后实际选择第三项，F6打开内联重命名，输入“恢复后的文件.txt”并提交。独立SFTP读取新路径内容C、旧路径不存在，列表显示中文新名称。move-reconnect-result.json全部通过，桌面和runner正常退出。

早先00db297e...已通过恢复/撤销但没有重命名；7ece8ad1...脚本误用F2，3d21717e...在输入框焦点尚未到达时发送Enter而打开文件。修正为实际F6，并明确等待document.activeElement为重命名输入框后，完整流程通过，不把这些测试错误计作产品修复。本轮无新生产修改。

## 原始标准对应

- 新建文件/目录：136，已重读真实Windows+Alpine create-desktop-result.json，中文/引号文件与中文目录、同名拒绝及原内容保持。
- 重命名：本轮F6中文重命名及独立SFTP读回；121/342补充无覆盖与不确定结果边界。
- 复制与移动：128，已重读真实Linux目录复制/移动、子文件内容和异地撤销报告；341补充真实SFTP多文件拖放移动。
- 删除：171，已重读实际Linux三文件删除报告及请求序列，第一项不重放、第二项权限失败、第三项在明确sudo之前未执行；164/168等补充回收站与单项删除。
- 批量/部分失败：171真实删除权限失败后继续剩余；341真实同名移动冲突停止后续且只撤销确认项；342及本轮覆盖响应丢失、真实连接恢复、不自动重试旧操作与明确处理剩余。

脚本 .cache/run-move-reconnect.cjs、move-reconnect-observer.cjs；日志 .cache/move-reconnect-desktop.log。使用当前342目录包，本轮无代码或依赖变更。

不声明任意服务器上的跨文件系统移动、绝对跨进程原子事务或用户未授权提权；这些限制已有对应错误和边界记录。F09、传输冲突/队列等标准继续独立验收。整体47/79，未推送或发布。
