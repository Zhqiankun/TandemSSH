# 双栏回收站入口、中文确认与真实删除恢复

2026-09-12。在 Windows 桌面连接隔离 Linux OpenSSH 进行复制/删除验收，发现打开本机面板后 FileManagerSidebar 被隐藏，唯一回收站入口随之不可见，删除后的恢复流程无法继续。

## 修改与文件责任

FileManager.tsx 在本机面板打开时的常驻工具区新增回收站按钮，复用既有 trashOpen 状态和 FileManagerTrashDialog，没有新后端接口或跨业务依赖。未连接时按钮禁用；侧栏可见时仍使用原入口。

use-confirmation.ts 的默认确认/取消标签使用已有 common.confirm/common.cancel 翻译，显式传入的标签继续保留。中文 fileManager.trash 从“垃圾”修正为“回收站”。回收站提示中沿用的 Termix 名称改为通用回收站描述，不改变磁盘目录命名或底座许可证声明。

## 实际桌面验证

最终报告：.cache/desktop-observation-report-344f948c-65d5-46d4-a45d-a04e2e7b30d7。

本轮从最新源码生成本地 win-unpacked，仅供验证，未上传为发布安装包。使用新临时桌面配置、新主机记录和隔离目录，核验 SSH 指纹；独立 SFTP 连接核对实际内容。

- 新版临时副本复制目录成功；切换到另一目录撤销副本，原目录内容不变。
- 移动目录后从另一目录撤销，位置和内容恢复。
- 中文删除确认中点击“取消”，原文件内容保持 fixture。
- 点击“确认”后原路径消失；双栏模式点击“回收站”，找到原文件行，点击“恢复”后原路径恢复且内容仍为 fixture。
- 应用正常退出，机器报告 cleanExit=true。测试远端目录由夹具清理。

delete-confirm-chinese-buttons.png 已实际查看，可见“回收站”和“确认/取消”。截图是在最后替换 Termix 提示文案之前，不能作为最终文案已重新运行的证据；该单句文案之后通过翻译键检查。

## 失败记录与验证限制

第一次报告 0eef4e10-d695-4a73-b242-2af57c3fc256 在寻找回收站入口超时；实际确认复制、移动、撤销及删除路径变化，最终不计通过。新增入口后第二次报告 84c997d2-766e-43a3-b177-8a961de538c5 仍超时，实际界面文本证明按钮显示为“垃圾”，修正翻译后第三次全流程通过。两次失败进程由观察器清理，不冒充正常退出。

本轮只验收普通单文件取消/删除/恢复和目录复制移动撤销；批量部分失败、sudo、断线后交互仍未全部验收，因此 B09 继续未全部验证。上述新 UI 修改不包含于已固定的 alpha.8 标签。
最终 tsc -b、翻译检查与 git diff --check 通过；相关源码 ESLint 零错误、仅 FileManager 原有 windowId 警告。隔离 Linux 实例经专属 QMP 关闭，启动器退出 0。
