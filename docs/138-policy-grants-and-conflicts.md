# 白名单预授权与全局拒绝冲突验收

2026-09-12，按文档 07 已批准语义：自动模式可以连续执行本任务预授权范围内的 allow/confirm 动作；collaborative 每个动作必须单次批准，不因任务预授权而取消逐项审核。

新增四种受控来源 agent/workflow/mcp/command-panel × 两种模式 × allow/confirm 的16种组合，每种检查未授权拒绝、已授权行为、参数超范围拒绝、第二项是否需批准，以及无关分组/主机/任务的严格拒绝集合不会影响当前目标。自动执行仅产生授权的 df -h；协作只执行被逐项批准的第一项，第二项没有写入。

A09 专项：OperationGateway 实际会话写入口在全局 deny、主机 allow 同时命中时拒绝，规则顺序互换也相同，写入数组为空。实际 PolicyEditor React 组件调用真实规则求值器，显示“规则拒绝”，全局/主机原因与双方命中来源同时可见，试算不保存、不接管、不执行。

gateway 与 PolicySettings 共37项通过；ESLint通过。以上证明A09具体的最终拒绝与来源显示，不冒充Windows实机或Codex协议端到端覆盖。结合前一轮32种deny矩阵，R08的统一网关行为证据已扩展，但完整规则管理持久化与桌面操作仍独立待验收。

本轮仅测试和证据，无生产行为或依赖变化；没有把自动模式已批准的预授权改成逐项弹窗，人工自由终端约定不变。
最终 tsc -b 类型检查通过。
