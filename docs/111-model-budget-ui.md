# 模型预算中文界面修复

2026-09-12。预算上限为 64，但原按钮始终显示“增加 10 次”，当前额度 60/63 时实际只增加 4/1，文案与请求不符。

AiTaskTranscript 统一计算 nextBudget=min(64,maxTurns+10)，按钮中英文通过 count 插值显示真实增量，请求使用同一 nextBudget。没有更改额度上限、服务端权限或恢复授权语义。UI 组件负责显示和动作编排，原 aiTaskApi / runner 仍负责协议与预算。

新增 5 项中文界面测试：20→30、60→64、63→64 的显示与 API 参数一致；64 时隐藏增加按钮并保留停止入口；请求失败显示中文错误并恢复按钮。预算错误提示始终明确“需要重新确认任务授权”。测试通过，ESLint 与中文缺失键检查通过（0 缺失）。

结合 110 的自动/协作实际任务运行时测试，预算耗尽后停止模型调用并归还控制权，增加额度本身不恢复请求，明确重新授权后才继续。生产层每次调用固定原 providerId/model，并检查配置 identity，无自动切换服务商路径。A24 可按模型调用次数预算的已确认范围标记 verified；这不是按服务商账单金额结算或撤销已经发出的付费请求。

公开 alpha.5 不含此次按钮文案修复；后续发行再包含。
tsc -b 类型检查通过。
