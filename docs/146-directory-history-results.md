# 目录传输历史的方向、阶段与条目结果

2026-09-12。目录条目处理可能成功完成“跳过”操作，因此外层status=succeeded不能单独表示文件已复制。历史DTO新增可选传输方向、目录阶段及条目结果；只从已记录的fileResult中投影枚举，不根据缺失数据推断。

方向限定upload/download；阶段限定preview/confirmed/entry；只有entry阶段读取created/merged/skipped/succeeded/failed/unknown。预览与确认阶段不展示条目状态。单文件传输可读取已记录方向，普通文本读写不套用目录字段。中文摘要分别显示“传输方向”“目录阶段”“条目结果”，例如“下载/条目执行/已跳过”，不会把跳过改成文件复制成功。

三文件32项通过：实际JSONL写入查询覆盖双向目录的八类阶段/结果记录、非法枚举拒绝投影，中文组件验证外层成功与条目跳过的区分，HTTP导出兼容性保持。ESLint与中文缺失键检查通过。此为记录投影/界面组件测试，不声称此次重新执行了真实目录传输或所有条目执行器。

责任边界保持：类型定义仅扩展可选摘要字段，reader校验，HistoryRecordSummary只读展示；不加入文件正文或本地授权秘密，不修改传输状态机、控制权或批准机制。完整桌面历史回放仍需验收。
最终 tsc -b 类型检查通过。
