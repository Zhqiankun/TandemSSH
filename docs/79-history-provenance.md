# 执行历史来源与目标摘要

状态：功能已实现，Windows 桌面专项验证通过；完整回归结果见下文。原始 F08 要求来源、目标、状态、输出、审批及策略版本可见。当前记录已有部分字段，但列表未展示；新操作上下文也未保存主机名称。

TaskRuntime 从任务的既有主机快照填充可选显示字段，不接收客户端提供的主机元数据；授权/审批从已验证 actor 保存来源。OperationContext 增加可选 hostId/hostName，仅用于审计显示，不进入现有执行摘要或授权判断。AuditHistoryReader 只投影已保存的字段，来源/模式/策略结论按枚举验证，输出再次脱敏并限长。旧记录缺失字段不推断为人工或 AI。

界面使用独立的只读摘要组件显示来源、模式、主机或会话、目录、策略版本/判断、退出码和输出片段；详情入口保留。不会因查看历史恢复控制权或执行动作。验证真实数据形状、旧记录缺省、未知值拒绝、脱敏/Unicode 限长、中文渲染及原任务执行回归。

## 验证证据与范围

历史投影、中文组件、任务执行与授权来源共 6 文件 / 74 项通过（`.cache/history-provenance-final-tests.log`）；类型检查通过，lint 为 0 错误 / 100 项既有警告；构建与本地开发目录包成功。新增组件只依赖历史 DTO 与 i18n；未增加共享抽象或修改执行授权摘要。

真实 Windows 客户端使用之前真实 Linux/MCP 验收留下的自有本地历史，在 SSH 服务离线时成功显示 MCP 来源、协作模式、策略版本、退出码 0 和脱敏输出。截图已查看，报告为 `.cache/history-provenance-report-c99b8ffd-cd8d-49ed-bc73-bd27bdc0b422/history-provenance-result.json`，客户端正常退出。旧操作没有主机快照时显示原有会话编号；旧审批缺少来源时显示“未记录”。这验证历史兼容和实际界面，不把旧记录补写为新数据。新任务主机快照及人工授权/审批来源由 TaskRuntime 集成测试覆盖。

F08 暂不标记完整验收：还需核对各记录入口的人/内置 AI/流程来源及目标字段完整性。当前改动不在公开 alpha.1 安装包内，后续安装包继续由 GitHub Actions 构建发布。

完整回归通过：512 文件通过、1 文件跳过；3638 项通过、12 项跳过，280.96 秒。机器结果 `.cache/history-provenance-regression-results.json`。本机完整回归排除真实 ConPTY 专项，该项仍由云端 Windows CI 执行。

## 生命周期记录补齐

任务创建、流程附加/完成、任务完成、恢复保存/恢复与取消现在从 TaskRuntime 私有 auditContext 读取同一可信任务快照。该方法仅服务本模块审计显示，不作为共享工具或鉴权入口；生命周期来源表示任务来源，人工审批来源仍单独记录为人工。补齐 sessionId/source/mode 以及显式完成状态，旧文件保持不改写。

新增行为验证覆盖 workflow/MCP/assistant 三种来源完成与人工取消，以及预先审核的计划自动完成。连同上传/下载目录恢复与审计读取，4 文件 / 60 项通过（`.cache/lifecycle-provenance-tests.log`）；完整类型检查及修改文件 ESLint 通过。首次测试因空授权范围触发 INVALID_TASK_GRANT，修正测试夹具为明确的 pwd 授权后通过，生产授权规则未放宽。恢复用例验证既有执行行为，未用其代替所有恢复字段的界面验收。此前 3638 项完整回归对应摘要提交 a489bf0，生命周期补齐的全库与真实 Windows 门禁由此次推送后的 CI 再执行。
