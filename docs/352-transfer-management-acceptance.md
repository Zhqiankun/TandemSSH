# 上传生命周期与 B06 传输管理验收

2026-09-14，当前Windows目录版，继续B06/F09。没有新增生产代码、共享抽象或依赖。

## 当前上传桌面实测

独立Windows客户端、真实loopback SSH/SFTP和本机来源文件。目录选择器仅固定返回本轮测试目录；操作通过本机面板选择、上传预览、队列按钮及真实协议写入完成。每个来源12,582,929字节。观察器只检查本轮SFTP服务端映射的独立目录。

- 实际进度文本为“已确认4194304 / 12582929 B · 1010 KiB/s · 预计剩余9秒”。点击暂停后无最终文件，暂停行隐藏旧ETA。校验后恢复完成，最终逐字节一致。
- 第二项传输中取消，最终目标不存在，远端.tandem-upload-*临时文件清理完成，已完成文件内容不变。
- 第三项在本次测试服务端第150次WRITE前注入一次错误。最终目标不存在，非零部分文件保留。点击“校验后恢复”后最终内容与来源一致，临时文件消失；第一项已完成文件的inode和mtimeMs不变。
- 完整文件SHA256：4961a77462ff43a7b6a85313e655314aabaad978c048b4c9c6295d2e51b71e54。

报告.cache/desktop-observation-report-1ff8246a-8fc4-488c-91ee-266ca41402ab/upload-lifecycle-result.json，日志.cache/upload-lifecycle-full-native.log。脚本.cache/run-upload-lifecycle-full.cjs、upload-lifecycle-full-scenario.txt。客户端正常退出，脚本exit0。查看了upload-failed-current.png；截图只有滚动区域的部分条目，不据此宣称全部行同时可见，恢复入口由实际按钮查找和点击验证。

首次脚本误等下载面板的“字节”措辞，上传实际为“已确认…B”，文件已完成但脚本未在传输中点暂停；修正断言后完整重跑。原失败报告7cfadd81-252d-4e4d-af83-22738520f7db保留。这不是产品传输失败。

故障注入只影响本轮SFTP夹具的一次WRITE，不等同真实远端磁盘耗尽。真实Linux磁盘容量边界仍由254证明。

## B06 原始条款审计

原始验收：队列、可配置并发、速度/字节数/预计剩余时间、暂停、恢复、取消和失败重试。

| 条款 | 直接依据 |
| --- | --- |
| 队列、可配置并发 | 349上传与350下载当前Windows桌面真实1→3→1，活动项不打断、待执行项按上限启动、五项字节完整 |
| 速度/字节数/预计剩余时间 | 351下载及352上传当前真实进度文本；118估算纯函数边界和实际中文面板测试 |
| 暂停与恢复 | 351当前下载和352当前上传暂停无最终文件、恢复后逐字节一致；39/40另有双向跨进程恢复证据 |
| 取消 | 351当前下载取消本机临时文件清理及已完成文件保留；352上传对应远端清理。351另修复远端取消响应不再阻塞本机清理 |
| 失败重试 | 257/258桌面下载单项及批次写入错误后恢复、实际字节正确；352当前上传受控WRITE失败后实际恢复且已完成文件不被重写 |

当前两侧队列、估算和面板联合回归4文件27项通过，日志.cache/transfer-management-final-regression.log；含351取消顺序修复测试。上述组合覆盖B06原始范围，仅B06标记verified，整体49/79。速度/ETA是估算，不承诺网络性能；F09完整文件管理与其余原条款继续保留。

未推送Git或触发Actions，公开安装包未更新。
