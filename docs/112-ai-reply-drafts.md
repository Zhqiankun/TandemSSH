# AI 问答草稿隔离

2026-09-12。TaskPanel 已按 agent.id 重新挂载任务对话，但同一任务的问题 ID 变化时原 answer 字符串会保留，导致旧回答出现在新问题下。另一个问题是 reply 请求成功后无条件清空文本，丢失请求期间用户继续编辑的草稿。

新增两项测试修复前均失败。现在 AiTaskTranscript 的草稿带 runId/questionId/text；只显示当前任务当前问题的草稿。发送成功仅在当前草稿仍匹配该次提交的身份及文本时清空。旧问题响应不能清空新问题草稿，也不会改变新问题的请求目标。未修改后台问答/授权契约，不新增共享模块或持久化格式。

回复仍只提交补充信息，不代表任务授权。问题切换时旧草稿不自动发送，发送期间编辑的文本保留供用户处理。

前端 AiTaskTranscript 7 项与后端 AI task-runner 13 项，共 20 项通过；报告 .cache/ai-reply-draft-results.json。修改文件 ESLint 通过。此修复尚未进入公开 alpha.5 包。
tsc -b 类型检查通过。
