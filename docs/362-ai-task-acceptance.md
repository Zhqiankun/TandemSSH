# F06 AI 任务验收归并

2026-09-14。原始F06：目标、步骤、操作建议、执行结果、有限轮次工具循环。本轮没有新增生产代码、API、共享抽象或依赖。

## 当前状态核对

A24预算条目已经在110/111完成。本轮读取其原始范围与当前runner，确认maxTurns整数递增、最高64、终态不得扩展、恢复持久化成功后才更新预算；不是重新实现预算或将普通聊天并发当任务预算。

当前运行task-runner、workflow-task、file-task、AiTaskComposer、AiTaskTranscript，5文件36项通过，日志.cache/ai-task-acceptance-regression.log。实际用例覆盖：规划不执行、授权后工具循环、等待协作批准不再次调用模型、agent不能授权自己或读其他任务、接管后旧工具作废、未知结果需人工核实、问答不轮询、上下文满时暂停、自动/协作两次调用预算耗尽及重新授权恢复、中文额度真实增量和上限/失败反馈。

## 原始条款对应

| 条款 | 实际证据 |
| --- | --- |
| 目标与会话绑定 | AiTaskComposer当前测试确保迟到创建结果仍绑定提交时会话；14实际任务目标/规划入口，247/267真实任务持续同一workflowRunId |
| 步骤与操作建议 | task-runner规划不派发及授权后run_command；247真实三步流程与协作逐条批准，模型只经父任务工具调度 |
| 执行结果 | 247/267真实Linux首步标记、未知步骤保留、跳过或重试终态、同一流程继续；271模型错误暂停且无远端操作，人工SSH仍可用 |
| 有限轮次工具循环 | 当前task-runner双模式预算测试及110：含规划2次调用耗尽暂停，归还人工控制权，扩预算后调度仍不调用，重新授权才继续；111中文界面额度和请求一致 |
| 工具与结果衔接 | workflow-task/file-task当前回归；247模型在人工控制期间不增加请求，恢复后查询原流程并完成；361实际流式工具往返和历史保存为提供商路径补充 |

本轮直接读取247的parent-matrix-result.json（fc933188）：自动/协作×跳过/重试4组合，工作流ID、首步单次执行、未知记录、等待人工及协作审批全部对应。也读取267的task-pause-result.json（eeb0df1b）：同4组合、独立暂停、HTTP校验、终态不复活和恢复语义正确。110/111分别为真实运行时配受控端口、中文实际组件验证，不冒充新的桌面额度耗尽录像。

预算指应用模型调用次数，不是服务商账单金额；不能撤销已经发出的请求。其他控制/安全验收项仍独立保留，不把本项完成推广为所有竞态和资源上限已验证。

据F06原始范围标记verified，整体56/79。R06授权范围内连续执行与其余安全边界继续独立归并；未推送Git或触发Actions，公开安装包未更新。
