# 下载跨重启检查点与恢复

2026-09-09，来源/原生恢复核心已实现，产品入口仍在实施。对应 B05/B06、A28/A29/A32：最终需要已暂停或异常中断的下载在重启后可发现，重新授权并核对两端后继续；同时保留后续上传、目录批次及 AI/MCP/流程恢复的完整范围。

## 本轮责任与依赖

- DownloadService 拥有远端来源检查点。只从已暂停、无并发读取的可信来源导出用户、目标身份、已接受公钥、规范路径、属性、整文件和分块摘要；不保存密码、旧连接 ID 或批准。恢复使用新连接重新检查身份和完整来源，返回新的预览 ID。
- DownloadSink 拥有本地部分文件检查点。持久化回调成功后才放开原能力和句柄，失败保留当前可恢复记录；只允许可信主进程调用恢复核心，不增加接受任意本地路径的渲染层入口。
- 原生恢复重新核对父目录、原有目标、临时文件身份、链接/硬链接、已确认块内容；目标发生变化或已确认块损坏时拒绝。返回暂停状态和新的能力 ID，不能自动继续或复用旧能力。
- 重启后未完成块尾部可以保留到恢复审阅阶段；只有确认续传后才沿用已有 resume 的已验证前缀截断逻辑。恢复预览本身不截断或提交文件。
- 密文检查点保管、用户身份/窗口私有通道、恢复清单 UI、任务重建与审批属于后续组合入口；仅核心通过不等于产品的重启续传已经交付。

实现前先固定源/目标检查点 schema，禁止把历史日志或客户端任意 JSON 当作新的授权。业务依赖方向为恢复协调器 → 来源/原生能力 → SFTP 或选定文件；加密持久化适配负责存取，不能反向调用执行流程。

## 验收与验证

1. 在本机真实 SSH/SFTP 下载第一块，导出检查点并结束旧来源/原生实例；新实例重新连接后验证来源和本地前缀，继续剩余内容并校验最终字节。
2. 原连接 ID、原生能力 ID 不能继续调用；恢复没有恢复旧授权。
3. 主机公钥、源文件内容/属性、规范路径、父目录、原目标或已确认块改变时拒绝恢复，不删除相关文件。
4. 持久化失败保留当前记录和部分文件；并发重复恢复、过大/畸形检查点和错误身份被拒绝。
5. 未知提交不能作为安全暂停重新执行；自动与协作继续沿用任务网关、人工接管和重新授权。

相关测试采用真实文件及回环 SFTP、失败注入和既有下载/自动协作回归。完整 UI 与实际应用重启证据另行记录，不能用对象重建测试替代整应用验收。

## 本轮已实现

新增 backend/files/download-checkpoint.ts 和 electron/download-checkpoint.cjs，分别约束服务端来源与原生部分文件的持久化数据。DownloadService.checkpoint 只导出已暂停来源，restore 用新请求与新连接完整校验，再建立新的预览。来源检查包含当前用户、实际目标身份、公钥、规范路径、文件属性、整文件和全部块摘要。

DownloadSink.suspend 在已暂停的已校验前缀上调用持久化适配，成功后才关闭并移除旧能力。持久化失败时当前记录和字节仍在；成功后即使句柄关闭异常，旧能力也不能再写，随后释放仍保留检查点部分文件。重启恢复要求新的授权守卫和重新核验的远端来源摘要，本地/远端检查点不匹配直接拒绝；覆盖已有目标必须重新确认。

恢复检查父目录与目标基线、生成临时文件名称/身份、链接和硬链接、已确认块；同一进程重复恢复同一个部分文件被拒绝。返回新能力并保持 paused，不自动派发剩余块；多出的未确认尾部仅报告长度，确认 resume 后才截断到已验证边界。Unknown 提交不能导出为安全暂停。

本轮没有新增渲染层/HTTP 的任意检查点导入接口；这些方法仅供后续可信协调器调用。检查点 schema 校验本身不构成授权，必须在加密存储和当前用户/窗口校验完成后使用。没有新建 common/shared 模块，来源规则仍属于 DownloadService，本地文件规则仍属于 DownloadSink。

## 实际验证

相关联合回归 **83 文件 / 573 项全部通过**，覆盖下载、原生目录、文件服务、AI/MCP、任务运行时、网关和中文协作面板；随后新增“重新确认覆盖”验收后的原生恢复专项 **8 项通过**。TypeScript、改动模块 lint、后端构建及本地 Windows 解包包通过。

独立进程场景已提交为 scripts/download-recovery.test.ts 与 scripts/test-helpers/download-recovery-worker.mjs：先通过真实回环 SSH/SFTP 获取首个 4 MiB 块并落盘，序列化检查点，销毁原服务和原生能力，然后启动新的 Node 进程及 SSH 连接。新进程重新核验来源、以新能力恢复剩余块，最终 SHA-256 和所有文件字节一致。旧预览/能力无法再次使用。测试凭据只通过测试子进程 stdin 传入，不写入检查点或命令参数。

负向场景覆盖持久化失败、重置与交接并发、持久化后句柄关闭失败、错误用户/公钥、源内容改变但属性相同、错配来源、目标替换、硬链接、已确认字节损坏、重复恢复、未确认尾部和未知提交。新进程测试使用真实文件与 SFTP，POSIX 所有者权限仍是 Windows 夹具模型，不代表真实 Linux/OpenSSH 矩阵全部通过。

最终包使用自身 Electron 运行时从 app.asar 加载新增原生模块，执行暂停交接 → 新能力恢复 → 校验提交，完成标记 packagedNativeRecovery=true、verifiedBytes=24。该探针验证打包后的原生核心；没有将其称作完整应用重启界面验收。

日志：.cache/download-recovery-regression.log、.cache/download-recovery-final-native-tests.log、.cache/download-recovery-types.log、.cache/download-recovery-lint-final.log、.cache/download-recovery-build.log、.cache/download-recovery-package.log、.cache/download-recovery-packaged-probe.log。

初次新增测试有一个夹具闭合括号错误，修正后实际执行；一次 4 MiB Buffer 的通用深比较耗时约 9 秒触发 5 秒测试超时，改为同等字节检查的 Buffer.equals 后整组约 2 秒通过，未放宽最终测试超时。没有通过跳过失败用例取得通过结果。

上一加密草稿提交 7b61d65 的 CI 34293592226 已成功，包含标准 Windows 原生构建与实际安装/卸载；本轮本机包复用现有原生模块，npmRebuild=false，云端标准原生流程保持原设置。

## 下一步组合入口

1. 将来源与原生检查点作为同一份受用户身份约束的加密记录提交，并记录可靠的占用/完成状态，不能只凭本地路径恢复。
2. 通过现有父子进程通道绑定真实用户、窗口及一次性票据；渲染层仅选择已有记录，不能提交任意本地路径或旧授权。
3. 增加中文恢复清单、服务器重新连接、来源/目标核对及新的覆盖确认；成功后交给现有队列继续，失败保留记录与明确原因。
4. 补齐完整应用重启/异常退出、上传与目录、AI/MCP/保存流程检查点及自动/协作重新授权的验收。

跨进程持久化占用、防重复恢复和恢复结果核对尚需由上述协调器实现；当前原生锁仅在同一实例内有效。整个 F01–F15 / B01–B16 / A01–A37 目标继续，不能据本轮核心通过宣称重启续传已经对用户可用。
