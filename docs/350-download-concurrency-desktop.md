# Windows 实际下载运行中并发调整

2026-09-14，继续B06/F09。没有修改产品调度、UI或预取行为，本轮只新增桌面验收场景。

当前Windows目录版连接真实loopback SSH/SFTP。五个独立来源分别为1MiB加1至5字节，内容不同，目标为本轮新建本机目录。通过真实下载预览、确认、并发控件、下载队列、原生文件写入完成；文件选择器仅固定返回本轮目录。服务端beforeRead按远端路径设置独立promise门控，覆盖任务的来源校验及后续下载读取。

先加入一个任务，等待队列控件出现后用原生输入事件设为1，再逐次选择其余四项并加入批次。上限1只有一个来源开始读取；改3出现三个独立来源；降1后放行其中两个并等到实际完成，第四项未开始、第三项未暂停/取消。放行第三项后第四项才开始，第四项完成后第五项开始。最后五项均完成，本机文件逐字节与服务端预置来源一致，每项SHA256保存在报告。

最终报告.cache/desktop-observation-report-17ca2c9b-a03a-4da4-a338-61b322fbe9a2/download-concurrency-result.json；日志.cache/download-concurrency-native-v7.log。脚本.cache/run-download-concurrency-native.cjs、download-concurrency-native-scenario.txt。客户端正常退出，验收脚本exit0。原下载队列回归1文件8项通过，日志download-concurrency-regression.log。

## 排查过程与证据界限

初次脚本在队列渲染前读取控件，随后一轮在独立首次启动引导等待处失败；均保留对应日志与失败快照。多选最初在同一个脚本事件内连续点击，实际仅保留部分选择，修正为逐次提交。

较小测试文件在上限1时出现两个远端读取的现象多次可复现；可见并发值确为1，但这些证据不足以证明调度超限。最终查明FileManager.prefetchFileIntent会在选择文件时经独立后台任务执行readSSHFile，最大预取范围512KiB；原128KiB测试文件将浏览预取与队列来源读取混在一起。改用超过该上限的1MiB文件，未关闭或修改产品预取功能，动态并发场景通过。此前日志download-concurrency-native.log、v2至v6.log保留，不能把这些脚本失败称为已修复产品故障。

本证据证明下载任务（含来源核对阶段）的真实动态调度和最终落盘，并不证明软件所有后台网络请求共用该下载并发上限；浏览预取属于独立功能。上传对应349，两侧动态并发已具备桌面证据。

B06还需将原条款中的队列、速度/字节/预计剩余时间、暂停、恢复、取消、失败重试的现有直接证据逐项审计，未据此提前完成整个B06；整体保持48/79。未推送Git或触发Actions。
