# 传输队列并发与部分失败复核

2026-09-12，B06/B05 组合验收。新增 UploadQueue 用例配置并发 2，添加三个不同目标文件，挂起前两个 prepare；第三个保持 queued，实际活跃数为 2。第一个预检失败后立即释放槽位并开始第三个，另外两个确认后完成；最终状态为 failed/completed/completed，路径顺序与每项记录保留。

本轮 UploadQueue 与 DownloadQueue 两文件 18 项通过，报告 .cache/transfer-queue-review-results.json。既有用例也覆盖暂停/取消、覆盖确认、未知提交不重复执行、原生释放失败保留记录等。旧上传面板测试产生 React act 警告，测试无失败；不将它描述为无警告运行。新增并发用例本身不挂载 React 面板。

仅增加测试，无生产模块或依赖变化。B06 的完整速度/剩余时间显示、双向动态并发及桌面多文件操作仍需逐项归并，因此本轮不标记整个条目完成。
新增测试 ESLint 与 tsc -b 类型检查通过。
