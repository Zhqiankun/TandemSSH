# A32：批次部分失败中文桌面汇总闭环

2026-09-13。

原始 A32：磁盘满、权限不足、传输部分失败，不假成功，已完成项与残留目标准确。

## 本轮桌面证据

实际 Windows 客户端 + loopback SSH/SFTP 下载目录 source，包含 source/empty 空目录、小文件 small.txt、大文件 large.bin。批次共四项（两个目录准备步骤、两个文件）。仅对本次目标目录内大文件暂存句柄的第 66 次写入注入一次 ENOSPC，其余文件照常写入。

成功报告 .cache/desktop-observation-report-aae9b0e8-e060-4498-934d-0c19157cf6ce。

失败后小文件实际完成、大文件只有部分暂存且最终目标不存在。中文批次汇总为“完成 3 / 4 项 · 跳过 0 · 失败 1 · 待核实 0”，失败条目的暂存路径与实际文件一致。点击该条目的“核对后恢复”后，汇总变为“完成 4 / 4 项 · 跳过 0 · 失败 0 · 待核实 0”。

恢复大文件 4,325,393 字节，SHA-256 与源数据一致；小文件仍为 already completed，inode 与 mtimeMs 不变。暂存文件消失，客户端正常退出，后台端口释放，夹具关闭。

batch-fault-result.json 已读取，batch-partial-failure.png 已实际查看，中文混合结果汇总可见；失败条目路径由 DOM 断言与文件系统交叉核对。截图并未同时展示滚动区域中的全部条目，不夸大截图范围。

前次 f4b61bd6-8b07-4710-b94f-f29daf092252 因脚本只数文件、漏数两个目录步骤而超时。实际 UI 四项正确，修改测试计数后新客户端完整重跑，没有改变生产统计逻辑。

## A32 证据对应

- 原子覆盖不支持错误按操作分类，避免误报：252。
- 真实本机暂存文件的 ENOSPC/权限错误注入、已校验前缀保护：253。
- 真实 Linux 1 MiB 限容保存失败、实际残留、原文件保持、清理后保存及真实权限拒绝：254。
- 上传三文件批次成功项与待执行项保护、部分写入失败后原 ID 恢复：255。
- 下载批次失败项不能登记完成、成功收据与文件保持、原 ID 恢复：256。
- 实际中文桌面的 ENOSPC/EACCES 提示、准确暂存路径、恢复后文件校验：257。
- 混合成功失败的中文批次汇总与实际文件一致：本轮。

组合满足 A32 原始范围，标记 verified。单一文件句柄错误注入不等同于本机磁盘真的耗尽；实际容量耗尽是隔离 Linux 254 的证据。此结论不替代整个文件功能 F09/B06–B10 的全部矩阵。

本轮没有生产代码、共享模块或依赖变化，只有隔离验收脚本与记录。未提交、推送或发布。
