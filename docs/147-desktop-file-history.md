# 旧目录传输历史的真实桌面回放

2026-09-12。使用最新本地 Windows 目录包，复用本项目此前目录流程验收的独立 profile .cache/desktop-observation-0200dc8f-2e79-43c0-923f-5569f33f9cb2；该数据来自历史 Windows/SFTP 夹具验收，不把它改称本轮 Linux 实传。本次未启动SSH测试服务器，也不执行新远端命令或传输。

先只读汇总原JSONL，确认其中存在24条目录操作结果：上传/下载各12条，预览4、确认4、条目16，另有2条命令结果。新客户端打开操作历史并翻到较早记录，最终两页可见上传、下载、预览、清单已确认、条目执行、已创建、成功与处理字节数。每个被截取的记录先滚入可视区并用elementFromPoint确认可见。

成功报告 .cache/file-history-report-9c04bb0d-19e3-4d8f-a879-29edccc96a48/file-history-desktop-result.json，pages=2、newRemoteActions=false。history-upload.png已查看：显示MCP、协作、会话编号、策略版本/判断、上传目录条目及已创建；旧审批缺少来源仍为“未记录”，不补写历史身份。其他方向和阶段截图同目录保存。客户端正常退出并释放后端端口，构建及本地目录打包通过。

首次837e4c46及第二次2338e345观察因脚本分页等待条件失败：字符串换行比较不一致，以及把加载时清空列表当成新页。改成等待非空新记录后通过，没有修改产品分页代码或原历史数据。

本轮证明旧实机目录记录与新摘要的实际桌面兼容。新失败原因/unknown判断等此前只有组件与真实日志链证据，尚未在此次旧数据里出现；不据此将R05/F08整体标记完成。
