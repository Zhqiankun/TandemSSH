# 流程中途接管后的恢复点验收

2026-09-12，A20专项。新增场景将三步流程的第一步完成、第二步printf实际写入后挂起，再由humanInput接管；此前测试主要在第一步被中断，不能替代已完成前缀不重跑的验证。

自动/协作两模式均确认：第二项保持unknown，未核对直接授权返回RECONCILIATION_REQUIRED，旧批准无效。人工明确选择skip后只执行第三步pwd -L；第一步pwd及其参数只出现一次。协作模式第三步再次等待批准，用第二步旧digest批准第三步被拒绝且没有写入，正确批准后才发送。最终三个原始状态为succeeded/unknown/succeeded，第二项保留reviewed.skip，流程为completed-with-errors。

父流程18项通过，已有中文恢复界面9项通过；TaskAuthorizationForm明确展示无默认值的跳过/重试选择，未选择时禁用授权。文档45已有四种Windows父流程正常重启恢复的历史实机证据，验证首文件时间戳不变及后续步骤继续；本轮不把它称为重新执行。

ESLint与tsc -b通过。本轮仅测试/证据，不改变生产恢复策略或增加依赖。A20“选择恢复点、不重跑已完成步骤”由当前运行时、界面校验及既有实机证据共同支持，标记verified；未知步骤不会被重写成成功，用户选择重试的副作用风险仍按原设计提示。
